# HugEngine 全路径追踪管线 — 完整规划

> ## ⚠️ 定位修订（2026-09-18）：PT 当前是「参考渲染器」，不是实时管线
>
> 代码现状已经把 PT 定位为**参考渲染器 / Ground Truth**（见 `Engine/Render/Pipeline/PathTracingPipeline.h`
> 的定位注释）：它不参与实时渲染、不使用 GI 层栈（GIConfig），收敛后的结果是渲染方程的数值解，
> 用作近似 GI 的判据（白炉能量守恒、DDGI 探针视角无关性、DDGI SH 评估修正、各档位亮度基准）。
>
> 因此本文档 §1.1 的 **Level 3「全 PT 实时管线」** 以及为实时性服务的整套路线
> （NRD/DLSS-RR 降噪、SER/OMM/Mega Geometry、NRC/NTC、33 周时间线、实时帧率目标）
> **从"目标"改为"条件触发项"** —— 只有决定把 PT 转为实时主路径时才启动。
> Level 1（混合 RT）与 Level 2（PT 参考模式）**均已落地**。
>
> **逐项复核结论、方案取舍与实测检索见新增的 §0 现状复核（2026-09-18）**。
> §1~§11 的原始设计内容全部保留（作为设计与储备），已完成项已就地标注 ✅。

> **原定位（2026-07-27 原文）**: 将渲染管线从光栅化主导升级为路径追踪主导，最终完全替代光栅化管线。
>
> **对标**: Cyberpunk 2077 RT Overdrive / Portal RTX / NVIDIA Zorah Demo / RTXDI SDK
>
> **前置条件**: [混合 RT 管线](../已实现功能/混合RayTracing管线规划.md) —— **已完成，且 RT 效果已并入 Deferred 层栈（HybridRTPipeline 已删除，见 §0 与附录 A）**
>
> **最后更新**: 2026-09-18（v1.2 现状复核）

---

## 目录

0. [现状复核（2026-09-18）](#0-现状复核2026-09-18)
1. [全路径追踪的定义](#1-全路径追踪的定义)
2. [整体架构](#2-整体架构)
3. [阶段 A: 路径追踪参考模式](#3-阶段-a-路径追踪参考模式)
4. [阶段 B: ReSTIR 时空重采样](#4-阶段-b-restir-时空重采样)
5. [阶段 C: NRD 降噪器集成](#5-阶段-c-nrd-降噪器集成)
6. [阶段 D: ReSTIR GI + ReSTIR PT](#6-阶段-d-restir-gi--restir-pt)
7. [阶段 E: 硬件加速优化](#7-阶段-e-硬件加速优化)
8. [阶段 F: 神经渲染加速](#8-阶段-f-神经渲染加速)
9. [阶段 G: 完整材质系统](#9-阶段-g-完整材质系统)
10. [实施时间线](#10-实施时间线)
11. [性能分析](#11-性能分析)
12. [后续任务清单（从 1 重新计数，2026-09-18）](#12-后续任务清单从-1-重新计数2026-09-18)

---

## 0. 现状复核（2026-09-18）

> 本节是 2026-09-18 对**代码现状**与**其它规划**的逐项复核结论，**它覆盖后续章节的目标设定**：
> §1~§11 的原始规划全部保留（设计与储备），但"是否还作为目标"以本节为准。
> 依据：`PathTracingPipeline.h` 的定位注释、`Engine/Render/RT/` 与 `PostProcess/` 的实际实现、
> `PTQualityCVars.h` 的真实 CVar 集合，以及对 `NRD/NRC/NTC/MegaGeometry/DLSS/SER/OMM` 的全库检索。

### 0.1 定位修订：参考渲染器，而非第四种实时管线

| PT 的当前用途（判据） | 说明 |
|---|---|
| 白炉能量守恒 | 校验层栈归一化合成的能量守恒 |
| DDGI 探针视角无关性 | M5.2-B 的判据 |
| DDGI SH 评估修正 | M5.3 的判据 |
| 各档位亮度基准 | 各 GI 组合的亮度对照 |

⇒ 与 `DeferredPipeline` 的关系是"**待测对象 vs 标准答案**"，不是"两条竞争管线"。
**Level 3 全 PT 实时化路线不再作为目标**（见 §0.3）。

### 0.2 已完成（对应正文各节，均为实测）

| 正文 | 状态 | 实际实现要点 |
|---|:---:|---|
| §3 阶段 A：PT 参考模式 | ✅ 2026-08-04 | `PT_Full.rgen/rchit/rmiss` + `PTPass`（**5** 个 UAV：HDR / 深度 / 法线+粗糙度 / 速度 / albedoMetallic）+ NEE + MIS + 俄罗斯轮盘赌 + 天空；`RTDenoiser` 时域累积；ToneMap/FXAA；ImGui 质量面板；CVar `r.PT.SPP/Bounces/MIS/Roulette/Denoise/Denoise.Blend` |
| §4 阶段 B：ReSTIR DI | ✅ 2026-08-04 | `ReSTIRPass`：Init→Temporal→Spatial 在**单个 RG Pass** 内顺序 dispatch（不靠 token 纹理强制排序）；蓄水池 SSBO 双缓冲 + 历史纹理双缓冲；PT RayGen 反弹 0 直接读 FinalReservoir + 1 条 shadow ray；CVar `r.PT.ReSTIR`（默认关）+ `r.PT.ReSTIR.Candidates/Radius/Samples` |
| §8.1 STBN | ✅ 2026-08-06 | `Tools/gen_stbn.py` → `STBNData.h` → `STBNTexture`（3D 纹理 + `Load()` 整数坐标）；**RT shader 内禁用组合采样器**（历史坑：会破坏材质绑定） |
| §5.3 A-Trous 空间滤波 | ✅ 2026-08-06 | `PTAtrousPass` + `PT_Atrous.comp.slang`：SVGF 风格 4 迭代 5-tap 十字核、深度/法线/亮度边权重、3×3 局部方差自适应 σ、可选火萤钳制；CVar `r.PT.Atrous*` 6 个 |
| （正文未列）粒子 | ✅ 2026-08-07 | `ParticleRenderer` 把 Billboard 粒子复合到 PT HDR（PT_Render 之后、时域降噪之前） |
| （正文未列）相机运动自适应时域 | ✅ | `RTDenoiser::SetMotionBlend` + 管线内相机位移/朝向检测：运动时抬升混合权重，缩短历史拖影 |
| （正文未列）加速结构共享 | ✅ | `PathTracingPipeline::SetSharedRTPass`：与 Deferred 复用同一份 BLAS/TLAS，避免重复内存 |
| §9 任务 1：材质链数据打通 | ✅ 2026-09-19 | `MeshComponent` 新增 Disney/折射字段（ior / anisotropic / subsurface / specular / specularTint / sheen / clearcoat / clearcoatGloss / transmission），glTF `KHR_materials_*` 扩展（ior / clearcoat / specular / sheen / anisotropy / transmission）映射进来；`RTPass::BuildSceneMaterialTexture` 场景材质纹理 **4→7 行**（row4=disneyA、row5=disneyB、row6=disneyC/F0/IOR/透射）；`PathPayload` **48→96B**（新增 disneyA / disneyB / surfaceParams），C++ 镜像 `RT/PathPayload.h` 用 `static_assert` + `offsetof` 钉住布局，`Tools/check_path_payload.py` 交叉校验两端字段名/顺序/偏移；doctest `TestPathPayload.cpp` 3 用例 |
| （正文未列）示例载体 | ✅ 2026-09-19 | `Samples/05.Sponza-PathTracing`：与 03/04 同一份 Sponza 场景改走 PT（完全不做光栅化），ImGui 暴露 §12 涉及的开关与参数，作为本清单所有任务的冒烟载体 |
| （正文未列）对照流程设施 | ✅ 2026-09-19 | `HE_DUMP_PT` 落盘（hdr/depth/normal/albedo）+ `Tools/pt/dump_pt.ps1`（私有 cfg 副本 + stale 守卫 + N 次运行）+ `Tools/pt/analyze_pt.py`（统计 / 离散度 / `--diff` 逐像素）；实测同帧两次运行逐像素差为 0 |
| §9 任务 4：transmission / volume | ✅ 2026-09-19 | `MeshComponent` 增 `attenuationColor/Distance`（KHR_materials_volume）；材质纹理 7→8 行（row7=σ_t）；`PathPayload` 96→112B（`volumeParams`）；`SampleBSDF` 增透射 delta 瓣（菲涅尔 + Snell + 全内反射，按能量份额分配分支概率）；折射后按 Beer-Lambert 解析解衰减；`TestPTVolumeMath.cpp` 对解析解验证；默认场景 dump 逐像素 0 差异 |
| 示例 05 暴露的两处缺陷 | ✅ 2026-09-19 | PT 降噪 pass 缺 `PreBind`/`BeginOffscreenPass`（黑屏）；RT/PT 材质只取因子（整场变纯金属）。根因、修复与验证见 **§0.6** |

### 0.3 方案取舍修订（原方案已不合适的地方）

| 原规划 | 修订为 | 原因（实测 / 规划依据） |
|---|---|---|
| §5 阶段 C：集成 NRD（ReBLUR/ReLAX/SIGMA） | **接入统一降噪框架**（`Lumen与Nanite完整设计规范` §5.1），NRD 降级为**可选外部后端** | 引擎现有 9 个降噪器实例 / 9 套 PSO / 14 张纹理，其中 `RTDenoiser` 就服务 5 处（含 PT）；统一框架的 11.1/11.2 已完成，11.3（按信号分派 + `alpha < 0` 有效性契约）待做。再单独插一套 NRD 会引入第三套信号分类与历史分配约定 |
| §5.4 DLSS Ray Reconstruction（Streamline SDK） | **条件触发**；引擎当前**无** Streamline/NGX 集成（检索 0 处） | 与"参考渲染器"定位不符：参考渲染器不需要实时降噪/超分 |
| §6 阶段 D：ReSTIR GI（统一估计器） | 落点改为 **Lumen / P6**（`Lumen…` §5.2 任务 20：P6 = ReSTIR GI 统一估计器 + 绑定数组化） | 该工作的验收对象是 Lumen 的"多信号共存"；在 PT 侧重复规划会形成两套估计器 |
| §6.2 ReSTIR PT（GRIS） | 保留为**条件项**（与 Level 3 同触发） | 属实时 PT 的收益项；参考渲染器靠多 SPP 收敛即可 |
| §7 阶段 E：SER / OMM / Mega Geometry | **条件触发**。现状：`RHI/Types.h` 的 `DeviceCaps` 只有 `supportsSER` / `supportsOMM` **字段**，未启用任何 Vulkan 扩展、无实现；Mega Geometry 检索 0 处 | 三者都是实时性能优化（重排序 / 透明微图 / 集群 LOD），对"慢一点无所谓"的参考渲染器收益有限；OMM 的价值更贴近 Nanite/虚拟几何（见 `Nanite_N1至N3实现计划.md`） |
| §8.2 NRC / §8.3 NTC | 不列入近期项；神经渲染能力已在 **AI 模块**（`Engine/AI/Neural`：`NeuralUpscaler`；另有 AIGC 与 GPU 推理后端） | NRC/NTC 检索 0 处；单列在 PT 规划里会与 AI 模块的落点重复 |
| §9.1 在 PT 里"重新实现" clearcoat/sheen/anisotropy 等 | 改为**复用 `pbr_common.slang` 已有的迪士尼 BRDF 求值**（`PBR_BRDF(..., disneyA, disneyB)` 已支持 anisotropic / subsurface / specular / sheen / clearcoat / clearcoatGloss / specularTint） | 光栅化与 RT 侧早已实现这些 lobe；PT 侧缺的是**采样端**（`PT_Common.SampleBSDF` 目前只有 metallic/roughness 的"余弦 + GGX"两瓣）与 **`PathPayload` 字段**（48B = albedoMetallic + normalRough + emissive，无 disney/IOR/transmission） |
| §10 实施时间线（33 周） | **失效**：A+B 实际约 1 周（07-27 立项 → 08-04 完成）、三件套 2 天；剩余项按 §0.4 的条件触发清单管理 | 原时间线按"实时全 PT"排期，与当前定位不符 |
| §11 性能目标（RTX 4070 @ 1440p 实时帧率） | 改为**参考模式的可用性目标**（交互可等待、结果可复现）；并记录已知约束：本机 Intel Arc 上 RT 管线编译约 20s/个，02.Cube 启动 80~110s | 同 §0.1 |

> **与 §12 的一致性（2026-09-18）**：§12 的清单按"**是否引入外部 SDK**"做过一次筛选 ——
> **12 NTC / 13 DLSS-RR 已删除**（libntc / Streamline+NGX），**10 Mega Geometry（仅需 `VK_NV_*` 扩展）
> 与 11 NRC（自研 GPU 推理后端）保留**为条件项。本节上表对这几项的"条件触发"描述依旧有效，
> 只是其中两项不再作为计划任务跟踪。

### 0.4 仍有效且未做（按优先级）

1. **材质 / BSDF 对齐**（§9）—— ✅ 全部完成（任务 1~4）：载荷 112B（Disney 参数 + 介质参数）、材质纹理 8 行、求值端复用共享 BRDF 且打包唯一、采样端三瓣 + 透射 delta 瓣、折射/介质按解析解实现；
2. **ReSTIR PT / GRIS**（§6.2）：条件触发（Level 3）；
3. **transmission / volume / iridescence / dispersion**（§9.1）：其中后两者需要光谱渲染改造，单独评估；
4. **性能基准设施** —— ✅ 已完成（任务 5/6）：`HE_DUMP_PT` 落盘 + `Tools/pt/dump_pt.ps1`（私有 cfg 副本、
   stale 守卫、N 次运行）+ `Tools/pt/analyze_pt.py`（统计 / 可复现性 / `--diff` / `--compare` / `--converge`）
   + `HE_DUMP_MODE=deferred` 的同场景对照模式；收敛判据与实验模板见 §12 任务 6 的完成记录。

### 0.5 复核实测事实（全库检索）

| 检索项 | `Engine/` 命中 |
|---|---|
| `NRD` | 0 |
| `NRC` / `NTC`（独立词） | 0（`NRC` 的 2 处命中在 `AI/Runtime/InferenceScheduler.h`，与神经辐射缓存无关） |
| `SER`（`supportsSER` / `invocation_reorder`） | 仅 `RHI/Types.h` 的 `DeviceCaps::supportsSER` 字段 |
| `OMM`（`supportsOMM` / `opacity_micromap`） | 仅 `RHI/Types.h` 的 `DeviceCaps::supportsOMM` 字段 |
| `MegaGeometry` / `mega_geometry` | 0 |
| `DLSS` / `NGX` | 0（imgui 的命中无关） |

### 0.6 缺陷修复记录（2026-09-19，由示例 05 暴露）

> 这两处缺陷共同造成「`05.Sponza-PathTracing` 渲染一片黑」。定位手法值得留档：
> 先给对照流程补了 `ldr` 落盘目标（ToneMap 之后、FXAA 之前，即屏幕内容），
> 一测就分出「渲染有问题」还是「显示链路有问题」——HDR 有内容而 LDR 全 0，
> 于是问题被锁定在降噪 → ToneMap 这一段，随后用 cfg 覆盖做二分（关降噪就正常）。

| # | 缺陷 | 根因 | 修复 | 验证 |
|:---:|---|---|---|---|
| 1 | 开启时域降噪后整屏全黑 | `PathTracingPipeline` 的 `PT_Denoise` pass 只做 `SetInputs + Render`，而 `RTDenoiser::Render` 内部只有 `Draw(3)`：既没有 `PreBind` 绑定降噪 PSO、也没有 `BeginOffscreenPass`，Draw 落在渲染通道之外（校验层 `VUID-vkCmdDraw-renderpass` / `-None-08606`），**降噪输出纹理从未被写入**；而 ToneMap 的输入正是这张纹理 → ToneMap 输出全 0 | 按 GI 侧既有约定（`RTProvider::PreBindAux` + 帧图的 `BeginOffscreenPass`）补成 `PreBind → BeginOffscreenPass → Render → EndOffscreenPass` | LDR 从全 0 变为正常画面（均值 0.53）；冒烟日志里上述两类校验告警消失；PT 的 depth/normal/albedo 等原始输出不受影响 |
| 2 | 场景呈"纯金属 + 均匀灰"、发暗且没有细节 | `RTPass` 的场景材质纹理只打包 glTF **因子**、完全忽略贴图。Sponza 的 25 个材质都是 `baseColorFactor=0.588` 灰、`metallicFactor` 缺省 **1.0**（真实金属度靠 metallicRoughness 贴图调制成石头）→ PT 把整个场景判成纯金属，漫反射项被 `(1-metallic)` 抹掉，只剩天空镜面反射 | `MeshComponent` 新增"有效材质"字段（`baseColorAvg` / `metallicAvg` / `roughnessAvg` / `hasMaterialAvg`），`RTPass` 在有均值时优先使用；示例在解码贴图时顺带算均值（基础色 RGB 均值 × 因子、metallicRoughness 的 B/G 均值 × 因子）。光栅化路径不受影响 | 命中像素 albedo 由恒定 `0.5918` 变为逐材质分布、metallic 由 ≈1.0 降下来；HDR 由"平场 0.27"变为有漫反射层次的结构（std 从 0.0007 量级升到 0.14） |

> 另：`05` 的 cfg 最初是从 `04_Sponza-Deferred.cfg` 复制的，带着 `light_enabled=0`（主方向光被关）
> 与 y≈205 的相机位置，会让首屏偏暗。已删除该 cfg，让 05 回到内置默认值（太阳开、相机在 Sponza 内）。

#### 0.6.1 同一轮里清掉的校验告警（2026-09-19）

顺带把 PT 链路里长期存在的校验告警清了一轮（`05` 冒烟，60s，Validation ON）：
**9 类 / 56 条 → 6 类 / 34 条**，其中三类已修复：

| 告警 | 根因 | 修复 |
|---|---|---|
| `vkCmdPushConstants-offset-01795`（2 类各 10 条） | RHI 的 `SetPushConstants` 按绑定类型给阶段掩码（图形=VS\|FS、RT=RGEN\|MISS\|CHIT\|AHIT\|CALL），而布局声明的范围更窄（`PTPass` 只声明 RGEN\|CHIT\|MISS，`ToneMap` 一类只声明 FS） | 在布局创建处把 stageFlags 拓宽到该管线种类的全体阶段（图形/计算/Mesh/RT + PSO 预编译共 6 处） |
| `VkImageMemoryBarrier-oldLayout-01208`（10 条） | `PT_Atrous` 是 compute 写的 storage image，帧图却声明成 `ResourceAccess::Write`，RenderGraph 据此发 `COLOR_ATTACHMENT_OPTIMAL` 屏障，而该图没有 COLOR_ATTACHMENT 用途 | 该 pass 的写访问改为 `ResourceAccess::UAV` |
| `vkCmdDraw-renderpass` / `-None-08606`（各 10 条） | 见上表缺陷 1（降噪 pass 未绑 PSO/未进离屏 Pass） | 同缺陷 1 |

仍剩两类（**告警级、渲染输出已验证正确**，本轮不动，留待单独处理）：

1. `VkImageMemoryBarrier-oldLayout-01197`（10 条）——某张后处理纹理首次使用时的屏障声明 `oldLayout=COLOR_ATTACHMENT_OPTIMAL`，
   而校验层对该句柄记录的"上一个已知布局"是 `PRESENT_SRC_KHR`（典型的句柄复用痕迹：启动期交换链/资源重建后
   `VkImage` 句柄被复用）。已做的加固：交换链 `Present` 后把该图像真实布局记为 `PRESENT_SRC`、视图销毁时清理追踪记录。
2. `vkCmdDraw-renderPass-02684`（10 条）——`BeginRenderPass(LoadOp::Load)` 的 LOAD 版 RenderPass **无条件带 D32 深度附件**
   （且依赖里含 EARLY_FRAGMENT_TESTS/DEPTH_STENCIL_WRITE），而 ImGui 的管线不带深度附件，二者不兼容。
   正解是让 LOAD 版 RenderPass 按 `depthFormat` 决定是否带深度（同时要改 Framebuffer 附件数与清除值个数），
   属于 RHI 的公共路径（02/03/04/06 都走它），改动面与回归风险较大，故本轮只记录诊断。

---

## 1. 全路径追踪的定义

### 1.1 进化路径

> **状态（2026-09-18）**：Level 1 ✅（RT 效果已并入 Deferred 层栈，HybridRTPipeline 已删除）；
> Level 2 ✅（`PathTracingPipeline`，`r.Pipeline.Mode=3`）；**Level 3 🔶 条件触发**（见 §0）。

```
Level 1: 混合 RT (已规划)
  RT 替代各个光栅化效果（阴影/反射/AO/GI），但主可见性仍是光栅化 GBuffer

Level 2: PT 参考模式
  完整路径追踪替代 Lighting Pass，但 GBuffer 仍是光栅化的
  可作为"质量参考"或截图模式

Level 3: 全 PT 管线
  路径追踪替代 GBuffer Pass（主可见性），光栅化仅保留 UI/调试
  ReSTIR + SER + OMM + NRC 全栈优化
```

### 1.2 各阶段核心差异

| 维度 | Level 2 (PT 参考) | Level 3 (全 PT) |
|------|:---:|:---:|
| 主可见性生成 | 光栅化 GBuffer | Path Tracing RayGen |
| 直接光照 | ReSTIR DI (多光源重要性采样) | 同 Level 2 |
| 间接光照 | PT 1-2 bounce | PT 2-4 bounce + ReSTIR GI |
| 阴影 | 包含在 PT 中 | 包含在 PT 中 |
| 反射 | 包含在 PT 中 (BSDF 采样) | 同 Level 2 |
| AO | 包含在 PT 中 | 包含在 PT 中 |
| 降噪 | NRD (ReBLUR/ReLAX) | DLSS Ray Reconstruction (Transformer) |
| 超分 | DLSS/FSR/XeSS (可选) | DLSS/FSR/XeSS (默认, 低内部分辨率) |
| 帧生成 | DLSS-FG (可选) | DLSS-MFG (可选) |
| 硬件加速 | 基础 | SER + OMM + DMM |
| 神经加速 | STBN (时空蓝噪声) | NRC + STBN |

> **状态（2026-09-18）**：本表的 **Level 2 列已基本落地** —— 唯一的偏差是"降噪"一行实际用的是
> 自研 `RTDenoiser`（时域累积）+ `PTAtrousPass`（A-Trous），不是 NRD；"超分/帧生成"未接入
> （无 DLSS/FSR/XeSS/FG 集成）。**Level 3 列全部未做**，且按 §0.1 已降级为条件触发项。

---

## 2. 整体架构

### 2.1 Level 3 全 PT 管线的 RenderGraph

```
全路径追踪管线的 BuildFrameGraph:

  GPU_Cull (AsyncCompute)
    ↓
  BLAS_Update (仅几何变更时)
    ↓
  TLAS_Build (每帧)
    ↓
  ReSTIR_InitialSampling (Compute Shader)
    对每个像素生成 M 个候选光源样本 → Reservoir
    ↓
  ReSTIR_TemporalReuse (Compute Shader)
    重投影历史 Reservoir → 合并
    ↓
  ReSTIR_SpatialReuse (Compute Shader)
    邻域像素共享 Reservoir 样本
    ↓
  PT_RayGen (RT Pass)  ← 主渲染 Pass
    └─ 发射主光线 → ClosestHit
       └─ 从 Reservoir 采样光源 → Shadow Ray (TraceRay)
       └─ BSDF 采样 → 间接光线 (TraceRay, 递归或迭代)
    └─ 输出: 噪声 HDR 颜色 + GBuffer (albedo/normal/depth/motionVec)
    ↓
  DLSS_RayReconstruction (或 NRD)
    从噪声 HDR + GBuffer 生成高质量降噪图像
    ↓
  ToneMap (HDR → LDR)
    ↓
  DLSS_SuperResolution (可选: 低内部分辨率 → 显示分辨率)
    ↓
  DLSS_FrameGeneration (可选: 从 LDR → 插值帧)
    ↓
  Present
```

### 2.2 与光栅化管线的共存

```
两个管线可共存于同一个引擎：

  if (r.Pipeline.Mode == 0) → DeferredPipeline（光栅化，现有）
  if (r.Pipeline.Mode == 1) → HybridRTPipeline（混合 RT）
  if (r.Pipeline.Mode == 2) → PathTracingPipeline（PT 参考）
  if (r.Pipeline.Mode == 3) → FullPTPipeline（全 PT）

  通过 ImGui 或 CVar 一键切换，底层共用 RTManager 的 AS 构建
```

---

## 3. 阶段 A: 路径追踪参考模式

> **状态（2026-09-18）：✅ 已完成**（2026-08-04）。实际实现与本文差异见 §0.2 与附录 C
> （5 个 UAV、单 pass 的 ReSTIR、ReSTIR 最终着色公式已修正等）。


### 3.1 目标

实现一个完整的路径追踪 RayGen Shader，理论上可以：
- 仅用 RayGen + ClosestHit + Miss 着色器渲染完整帧
- 支持任意 bounce 深度
- 支持直接光照 NEE (Next Event Estimation)
- 支持 MIS (Multiple Importance Sampling)
- 支持俄罗斯轮盘赌终止

### 3.2 核心着色器

```hlsl
// FullPT.rgen.slang — 全路径追踪 RayGen

struct PTPushConstant {
    float4x4 invViewProj;
    float3   cameraPos;
    uint     maxBounces;       // 最大弹射次数 (4-8)
    uint     sampleCount;      // 每像素 SPP
    uint     frameIndex;
    float    skyIntensity;
    uint     flags;            // bit0=MIS, bit1=NEE, bit2=RussianRoulette
};

[[vk::binding(0, 0)]] RaytracingAccelerationStructure g_TLAS;
[[vk::binding(1, 0)]] RWTexture2D<float4> g_Output;

// 场景数据（如果 SSBO 已修复）
[[vk::binding(2, 0)]] StructuredBuffer<GPUObjectData> g_Objects;
[[vk::binding(3, 0)]] StructuredBuffer<GPULight> g_Lights;
[[vk::binding(4, 0)]] uint g_LightCount;

// 天空环境光
[[vk::binding(5, 0)]] TextureCube<float4> g_SkyEnv;
[[vk::binding(6, 0)]] SamplerState g_Sampler;

struct PathPayload {
    float3 worldPos;
    float3 normal;
    float3 albedo;
    float  metallic;
    float  roughness;
    float  emissiveR, emissiveG, emissiveB;  // 命中点自发光
    uint   materialID;
    float  hitT;
    uint   instanceID;
};

[shader("raygeneration")]
void main() {
    uint2 idx = DispatchRaysIndex().xy;
    uint2 dim = DispatchRaysDimensions().xy;
    
    float3 radiance = float3(0, 0, 0);
    float3 throughput = float3(1, 1, 1);
    
    // 初始光线
    float2 jitter = BlueNoiseSample(idx, g_PC.frameIndex);
    float2 uv = (float2(idx) + jitter) / float2(dim);
    RayDesc ray = GenerateCameraRay(uv, g_PC.invViewProj, g_PC.cameraPos);
    
    for (uint bounce = 0; bounce < g_PC.maxBounces; bounce++) {
        PathPayload payload;
        payload.hitT = -1;
        payload.emissiveR = 0;
        payload.emissiveG = 0;
        payload.emissiveB = 0;
        
        TraceRay(g_TLAS, RAY_FLAG_NONE, 0xFF, 0, 0, 0, ray, payload);
        
        if (payload.hitT < 0) {
            // Miss: 采样天空/环境光 → 终止
            radiance += throughput * SampleSky(ray.Direction);
            break;
        }
        
        // 累积自发光
        radiance += throughput * float3(payload.emissiveR, payload.emissiveG, payload.emissiveB);
        
        // 读取材质
        MaterialData mat = MaterialFromPayload(payload);
        float3 N = payload.normal;
        float3 V = -ray.Direction;
        
        // NEE: 直接光照采样
        if (g_PC.flags & FLAG_NEE) {
            float3 directLight = SampleDirectLight_MIS(
                payload.worldPos, N, V, mat,
                g_Lights, g_LightCount,
                g_TLAS,  // 用于 shadow ray
                g_PC.frameIndex, idx, bounce
            );
            radiance += throughput * directLight;
        }
        
        // 俄罗斯轮盘赌终止
        if (g_PC.flags & FLAG_RUSSIAN_ROULETTE) {
            float survivalProb = min(max(throughput.x, max(throughput.y, throughput.z)), 1.0);
            if (bounce > 3 && BlueNoiseRandom(idx, g_PC.frameIndex, bounce) > survivalProb)
                break;
            throughput /= survivalProb;
        }
        
        // BSDF 采样 → 下一根光线
        float3 wi;
        float pdf;
        float3 bsdf = SampleBSDF(N, V, mat, wi, pdf);
        throughput *= bsdf / pdf;
        
        ray.Origin = payload.worldPos + N * 0.001;
        ray.Direction = wi;
        ray.TMin = 0.01;
        ray.TMax = 10000.0;
    }
    
    g_Output[idx] = float4(radiance, 1.0);
}
```

### 3.3 ClosestHit 着色器

```hlsl
// FullPT.rchit.slang — 路径追踪 ClosestHit

[shader("closesthit")]
void main(inout PathPayload payload) {
    uint instanceID = InstanceID();
    uint primitiveID = PrimitiveIndex();
    float2 barycentrics = HitTriangleBarycentrics();
    
    // 读取物体数据
    GPUObjectData objData = g_Objects[instanceID];
    
    // 读取材质
    MaterialData mat = LoadMaterial(objData.materialID);
    
    // 插值顶点属性 (需要 SSBO 或 position_fetch)
    float3 worldPos = WorldRayOrigin() + WorldRayDirection() * RayTCurrent();
    float3 N = HitTriangleVertexNormal(0) * barycentrics.x
             + HitTriangleVertexNormal(1) * barycentrics.y  
             + HitTriangleVertexNormal(2) * barycentrics.z;
    
    // 法线变换到世界空间
    N = normalize(mul(float4(N, 0), objData.worldMatrix).xyz);
    
    // 填充 payload
    payload.worldPos = worldPos;
    payload.normal = N;
    payload.albedo = mat.baseColor;
    payload.metallic = mat.metallic;
    payload.roughness = mat.roughness;
    payload.emissiveR = mat.emissive.x;
    payload.emissiveG = mat.emissive.y;
    payload.emissiveB = mat.emissive.z;
    payload.materialID = objData.materialID;
    payload.hitT = RayTCurrent();
    payload.instanceID = instanceID;
}
```

### 3.4 关键算法: NEE + MIS

```hlsl
// 直接光照采样 (Next Event Estimation + MIS)
float3 SampleDirectLight_MIS(
    float3 pos, float3 N, float3 V, MaterialData mat,
    StructuredBuffer<GPULight> lights, uint lightCount,
    RaytracingAccelerationStructure tlas,
    uint frameIdx, uint2 pixelIdx, uint bounce)
{
    float3 directLight = float3(0, 0, 0);
    
    for (uint li = 0; li < lightCount; li++) {
        GPULight light = lights[li];
        
        // --- 光源采样 (Light Sampling) ---
        float3 lightPos; float lightPdf;
        SampleLightPosition(light, pos, N, lightPos, lightPdf,
                            frameIdx, pixelIdx, bounce, li);
        float3 L = normalize(lightPos - pos);
        float dist = length(lightPos - pos);
        
        // BRDF 评估
        float3 brdf = EvaluateBRDF(mat, N, V, L);
        float brdfPdf = GGX_Pdf(V, N, L, mat.roughness);
        
        // Shadow Ray
        RayDesc shadowRay;
        shadowRay.Origin = pos + N * 0.001;
        shadowRay.Direction = L;
        shadowRay.TMin = 0.01;
        shadowRay.TMax = dist - 0.01;
        
        ShadowPayload spayload;
        spayload.blocked = false;
        TraceRay(tlas, 
            RAY_FLAG_ACCEPT_FIRST_HIT_AND_END_SEARCH |
            RAY_FLAG_SKIP_CLOSEST_HIT_SHADER,
            0xFF, 0, 0, 0, shadowRay, spayload);
        
        if (spayload.blocked) continue;  // 被遮挡
        
        // Power Heuristic MIS weight
        float misWeight = PowerHeuristic(lightPdf, brdfPdf);
        
        // 辐射度计算
        float3 Li = light.color * light.intensity / (dist * dist);
        directLight += brdf * Li * misWeight / lightPdf;
        
        // --- BRDF 采样 (BSDF Importance Sampling) ---
        // 如果光源在 BRDF 采样的方向附近，也给 MIS 权重
        // ... (MIS 第二部分)
    }
    
    return directLight;
}
```

### 3.5 天空与环境光

```hlsl
float3 SampleSky(float3 direction) {
    // 方案 1: 渐变天空 (简单)
    float t = 0.5 * (direction.y + 1.0);
    return lerp(float3(0.15, 0.12, 0.08), float3(0.55, 0.58, 0.62), t);
    
    // 方案 2: HDRI 环境贴图 (高质量)
    // return g_SkyEnv.SampleLevel(g_Sampler, direction, 0).rgb * g_PC.skyIntensity;
    
    // 方案 3: Bruneton 物理天空 (最真实)
    // return EvaluateAtmosphericScattering(direction, sunDirection, ...);
}
```

---

## 4. 阶段 B: ReSTIR 时空重采样

> **状态（2026-09-18）：✅ ReSTIR DI 已完成**（2026-08-04）；ReSTIR GI / ReSTIR PT（§6）未做，
> 且 GI 部分的落点已改为 Lumen/P6（见 §0.3）。


### 4.1 算法概述

ReSTIR (Reservoir-based Spatio-Temporal Importance Resampling) 是 RTXDI SDK 的核心算法。它将直接光照采样问题转化为 WRS (Weighted Reservoir Sampling) 问题。

```
核心思想: 每个像素维护一个 Reservoir（样本池）
  Reservoir = { selectedLight, weightSum, M (可见样本数), W (权重和) }

每帧三个 Pass:
  1. Initial Sampling: 每像素随机采样 M 个光源候选 → 评估 → 存入 Reservoir
  2. Temporal Reuse: 从上一帧重投影位置读取 Reservoir → 合并
  3. Spatial Reuse: 从邻域像素读取 Reservoir → 合并

最终: 从 Reservoir 中采样 1 个光源 → Shadow Ray → 着色
```

### 4.2 着色器实现

#### Pass 1: Initial Sampling

```hlsl
// ReSTIR_Init.comp.slang — 初始采样 + Reservoir 构建

struct Reservoir {
    uint   lightIndex;         // 选中的光源索引
    float  weightSum;          // 权重和
    uint   M;                  // 累计样本数
    float  W;                  // 选中样本的权重
    float3 lightPos;           // 采样点的世界位置（阴影光线终点）
};

// 输入: GBuffer (worldPos, normal, roughness, metallic, albedo)
// 输出: InitialReservoir (RWStructuredBuffer)
// 备选: 输出到 RT 纹理 (float4×2)

[numthreads(8, 8, 1)]
void main(uint3 idx : SV_DispatchThreadID) {
    // 读取 GBuffer
    float3 worldPos = g_WorldPos[idx.xy];
    float3 N = g_Normal[idx.xy];
    float roughness = g_Roughness[idx.xy];
    float3 V = normalize(g_CameraPos - worldPos);
    MaterialData mat = LoadMaterial(g_MaterialID[idx.xy]);
    
    // 初始化 Reservoir
    Reservoir reservoir;
    reservoir.lightIndex = 0;
    reservoir.weightSum = 0;
    reservoir.M = 0;
    reservoir.W = 0;
    reservoir.lightPos = float3(0, 0, 0);
    
    // 候选样本数 (通常 4-32)
    uint M = g_CandidateSamples;
    
    for (uint i = 0; i < M; i++) {
        // 随机选择一个光源
        uint li = RandomInt(idx, g_FrameIndex, i) % g_LightCount;
        GPULight light = g_Lights[li];
        
        // 在光源上随机采样一个点
        float3 lightPos;
        float lightPdf;
        SampleLightPosition(light, worldPos, N, lightPos, lightPdf,
                           idx, g_FrameIndex, i);
        
        float3 L = normalize(lightPos - worldPos);
        float dist = length(lightPos - worldPos);
        
        // BRDF 评估
        float3 brdf = EvaluateBRDF(mat, N, V, L);
        float p_hat = luminance(brdf * light.color * light.intensity / (dist * dist)) / lightPdf;
        
        // WRS 更新
        reservoir.weightSum += p_hat;
        reservoir.M++;
        
        // 以概率 p_hat / weightSum 替换当前选中样本
        if (RandomFloat(idx, g_FrameIndex, i) * reservoir.weightSum < p_hat) {
            reservoir.lightIndex = li;
            reservoir.W = p_hat;
            reservoir.lightPos = lightPos;
        }
    }
    
    g_InitialReservoir[idx.xy] = reservoir;
}
```

#### Pass 2: Temporal Reuse

```hlsl
// ReSTIR_Temporal.comp.slang — 时域重采样

[numthreads(8, 8, 1)]
void main(uint3 idx : SV_DispatchThreadID) {
    Reservoir current = g_InitialReservoir[idx.xy];
    
    // 读取 Motion Vector
    float2 mv = g_MotionVector[idx.xy];
    float2 historyUV = (float2(idx.xy) - mv) / float2(g_Width, g_Height);
    int2 historyIdx = int2(historyUV * float2(g_Width, g_Height));
    
    // 边界检查
    if (historyIdx.x < 0 || historyIdx.x >= g_Width ||
        historyIdx.y < 0 || historyIdx.y >= g_Height) {
        g_TemporalReservoir[idx.xy] = current;
        return;
    }
    
    // 时域验证
    float3 historyPos = g_HistoryWorldPos[historyIdx];
    float3 historyN = g_HistoryNormal[historyIdx];
    if (distance(g_WorldPos[idx.xy], historyPos) > 0.1 ||
        dot(g_Normal[idx.xy], historyN) < 0.9) {
        g_TemporalReservoir[idx.xy] = current;
        return;
    }
    
    // 合并两个 Reservoir
    Reservoir history = g_TemporalReservoir_History[historyIdx];
    
    // 目标函数评估: 对当前世界位置评估历史光源
    float p_hat_current = EvaluateLightTarget(
        g_WorldPos[idx.xy], g_Normal[idx.xy], g_Roughness[idx.xy],
        g_Lights[current.lightIndex], current.lightPos);
    current.W *= p_hat_current;  // 修正当前 Reservoir 的目标函数
    
    float p_hat_history = EvaluateLightTarget(
        g_WorldPos[idx.xy], g_Normal[idx.xy], g_Roughness[idx.xy],
        g_Lights[history.lightIndex], history.lightPos);
    history.W *= p_hat_history;  // 修正历史 Reservoir 的目标函数
    
    // WRS 合并
    g_TemporalReservoir[idx.xy] = CombineReservoirs(current, history);
}
```

#### Pass 3: Spatial Reuse

```hlsl
// ReSTIR_Spatial.comp.slang — 空间重采样

[numthreads(8, 8, 1)]
void main(uint3 idx : SV_DispatchThreadID) {
    Reservoir center = g_TemporalReservoir[idx.xy];
    float3 centerPos = g_WorldPos[idx.xy];
    float3 centerN = g_Normal[idx.xy];
    
    // 空间采样半径 (2-5 像素)
    const uint radius = 3;
    const uint sampleCount = 5;
    
    for (uint i = 0; i < sampleCount; i++) {
        // 在邻域中随机采样
        int2 offset = int2(
            RandomInt(idx, g_FrameIndex + 100, i * 2) % (radius * 2 + 1) - radius,
            RandomInt(idx, g_FrameIndex + 200, i * 2 + 1) % (radius * 2 + 1) - radius
        );
        int2 neighborIdx = int2(idx.xy) + offset;
        
        // 边界检查
        if (neighborIdx.x < 0 || neighborIdx.x >= g_Width ||
            neighborIdx.y < 0 || neighborIdx.y >= g_Height)
            continue;
        
        Reservoir neighbor = g_TemporalReservoir[neighborIdx];
        float3 neighborPos = g_WorldPos[neighborIdx];
        float3 neighborN = g_Normal[neighborIdx];
        
        // 几何验证（拒绝不相似的邻域）
        if (distance(centerPos, neighborPos) > 0.1 ||
            dot(centerN, neighborN) < 0.9)
            continue;
        
        // 重新评估邻域 Reservoir 对当前像素的目标函数
        float p_hat = EvaluateLightTarget(
            centerPos, centerN, g_Roughness[idx.xy],
            g_Lights[neighbor.lightIndex], neighbor.lightPos);
        neighbor.W *= p_hat;
        
        // WRS 合并
        center = CombineReservoirs(center, neighbor);
    }
    
    g_FinalReservoir[idx.xy] = center;
}
```

#### Pass 4: Final Shade

```hlsl
// ReSTIR_Shade.comp.slang — 最终着色

[numthreads(8, 8, 1)]
void main(uint3 idx : SV_DispatchThreadID) {
    Reservoir reservoir = g_FinalReservoir[idx.xy];
    
    if (reservoir.M == 0) {
        g_DirectLight[idx.xy] = float4(0, 0, 0, 0);
        return;
    }
    
    GPULight light = g_Lights[reservoir.lightIndex];
    float3 worldPos = g_WorldPos[idx.xy];
    float3 N = g_Normal[idx.xy];
    float3 V = normalize(g_CameraPos - worldPos);
    MaterialData mat = LoadMaterial(g_MaterialID[idx.xy]);
    
    float3 L = normalize(reservoir.lightPos - worldPos);
    float dist = length(reservoir.lightPos - worldPos);
    
    // Shadow Ray
    RayDesc shadowRay;
    shadowRay.Origin = worldPos + N * 0.001;
    shadowRay.Direction = L;
    shadowRay.TMin = 0.01;
    shadowRay.TMax = dist - 0.01;
    
    ShadowPayload spayload;
    spayload.blocked = false;
    TraceRay(g_TLAS,
        RAY_FLAG_ACCEPT_FIRST_HIT_AND_END_SEARCH |
        RAY_FLAG_SKIP_CLOSEST_HIT_SHADER,
        0xFF, 0, 0, 0, shadowRay, spayload);
    
    if (spayload.blocked) {
        g_DirectLight[idx.xy] = float4(0, 0, 0, 0);
        return;
    }
    
    // 着色
    float3 brdf = EvaluateBRDF(mat, N, V, L);
    float3 Li = light.color * light.intensity / (dist * dist);
    
    // 无偏估计（RTXDI 规范）：f(y) · W · M / weightSum
    // ⚠️ 本文原写法 W/(weightSum·M) 缺 M 因子（亮度会严重偏暗）—— 实现时已按此式修正，
    //    现就地改正（见附录 C「已知问题 3」）。
    float3 result = brdf * Li * reservoir.W * reservoir.M / reservoir.weightSum;
    
    g_DirectLight[idx.xy] = float4(result, 1.0);
}
```

### 4.3 配置参数

| 参数 | Level 2 (PT 参考) | Level 3 (全 PT) |
|------|:---:|:---:|
| 候选光源数 (M) | 8 | 16-32 |
| 时域复用 | ✅ | ✅ |
| 空间复用半径 | 3 | 5 |
| 空间复用采样数 | 5 | 10 |
| 最终 Shadow Ray | 1 spp | 1-2 spp (BRDF 检验) |
| 有效样本数 | ~100+ per frame | ~500+ per frame |

---

## 5. 阶段 C: NRD 降噪器集成

> **状态与修订（2026-09-18）：方案已改**。当前引擎的 PT 降噪 = **自研 `RTDenoiser`（时域累积，
> 含相机运动自适应混合）+ `PTAtrousPass`（A-Trous 空间滤波）**，两者均已实现（2026-08-06）。
> 后续不单独集成 NRD，而是**接入统一降噪框架**（`Lumen与Nanite完整设计规范` §5.1：11.1/11.2 已完成，
> 11.3 按信号分派 + `alpha < 0` 有效性契约待做）；NRD 作为该框架下的**可选外部后端**保留。
> 理由：再插一套 NRD 会引入第三套信号分类与历史分配约定（§0.3）。


### 5.1 NRD 集成架构

```
NRD (NVIDIA Real-time Denoisers) 提供三个降噪器:

  ReBLUR Diffuse   → PT GI (间接漫反射降噪)
  ReBLUR Specular  → PT Reflection (间接镜面降噪)
  SIGMA            → RT Shadow (阴影降噪)
  ReLAX            → ReSTIR DI (时空重采样信号降噪)
```

### 5.2 集成步骤

```
1. 下载/编译 NRD 库 → 链接到 HugEngine
   (https://github.com/NVIDIAGameWorks/RayTracingDenoiser)

2. 创建 Vulkan NRD 上下文
   - 初始化 nrd::Instance
   - 配置: CommonSettings (分辨率, 帧索引, 时间)
   - 配置: ReBLUR/ReLAX/SIGMA 各自的 Settings

3. 每个 RT Pass 后插入 NRD 降噪 Pass:
   
   RT_Reflection (噪声) → ReBLUR Specular Denoise → 降噪反射
   RT_GI (噪声)         → ReBLUR Diffuse Denoise  → 降噪 GI
   ReSTIR DI 输出       → ReLAX Denoise            → 降噪直接光照
   RT_Shadow (噪声)     → SIGMA Denoise            → 降噪阴影

4. NRD 输入: 
   - IN_DIFF_RADIANCE_HITDIST (颜色 + 命中距离)
   - IN_NORMAL_ROUGHNESS (GBuffer normal + roughness)
   - IN_VIEWZ (线性深度)
   - IN_MV (motion vector, 用于时域累积)

5. NRD 输出:
   - 降噪后纹理 → 直接输入到 Lighting/ToneMap
```

### 5.3 自研降噪方案（**已实现，当前主线**）

> **状态（2026-09-18）：✅ 已实现**。已落地的形态与下面这份示例代码有差异，以实际实现为准：
> `PTAtrousPass` + `PT_Atrous.comp.slang`（SVGF 风格 4 迭代、5-tap 十字核、深度/法线/亮度边权重、
> 3×3 局部方差自适应 σ、可选火萤钳制），配合 `RTDenoiser` 的时域累积
> （`r.PT.Denoise` / `r.PT.Denoise.Blend` / `r.PT.Atrous*` 共 8 个 CVar）。
> 它**不是"NRD 不可用时的回退"，而是当前唯一在用的降噪路径**；A-Trous 依赖时域降噪输出，
> `r.PT.Denoise` 关闭时自动旁路。


```hlsl
// SVGF_Atrous.comp.slang — A-Trous 小波滤波

[numthreads(8, 8, 1)]
void main(uint3 idx : SV_DispatchThreadID) {
    float3 centerColor = g_Input[idx.xy].rgb;
    float centerDepth = g_Depth[idx.xy];
    float3 centerN = g_Normal[idx.xy];
    
    const float sigmaColor = 0.5;
    const float sigmaDepth = 0.02;
    const float sigmaNormal = 0.1;
    
    // 5×5 A-Trous 滤波
    float3 sum = float3(0, 0, 0);
    float totalWeight = 0;
    
    const int kernel[25][2] = { /* 5×5 offset pattern */ };
    const float stepSize = pow(2.0, g_FilterIteration); // 每轮步长翻倍
    
    for (uint i = 0; i < 25; i++) {
        int2 sampleIdx = int2(idx.xy) + int2(kernel[i][0], kernel[i][1]) * int(stepSize);
        
        // 边界检查
        if (sampleIdx.x < 0 || sampleIdx.x >= g_Width ||
            sampleIdx.y < 0 || sampleIdx.y >= g_Height)
            continue;
        
        float3 sampleColor = g_Input[sampleIdx].rgb;
        float sampleDepth = g_Depth[sampleIdx];
        float3 sampleN = g_Normal[sampleIdx];
        
        // 边缘感知权重
        float wColor = exp(-length(sampleColor - centerColor) / sigmaColor);
        float wDepth = exp(-abs(sampleDepth - centerDepth) / sigmaDepth);
        float wNormal = pow(max(0.0, dot(sampleN, centerN)), 128.0);
        
        float w = wColor * wDepth * wNormal;
        sum += sampleColor * w;
        totalWeight += w;
    }
    
    g_Output[idx.xy] = float4(sum / max(totalWeight, 0.001), 1.0);
}
```

### 5.4 DLSS Ray Reconstruction（**条件触发**）

> **状态（2026-09-18）**：未做，且引擎**没有** Streamline/NGX 集成（检索 0 处）。
> 与 §5 一样：它服务"实时 PT"，在 PT 作为参考渲染器期间不启动（§0.3）。



```
DLSS-RR (DLSS 3.5+, 2023):
  输入: 噪声 PT 输出 (1 spp) + GBuffer (albedo/normal/depth/motionVec) + 光源/材质信息
  输出: 高质量降噪图像 (相当于 128-256 spp 的离线渲染质量)

  Transformer 架构自注意力机制 → 比 NRD 单帧降噪更好的细节保留
  参考: NVIDIA DLSS-RR 论文 (SIGGRAPH 2023)

  集成: 通过 Streamline SDK
  CVar: r.Streamline.RayReconstruction (1=DLSS-RR, 0=NRD/自研)
```

---

## 6. 阶段 D: ReSTIR GI + ReSTIR PT

> **状态与落点修订（2026-09-18）**：ReSTIR DI 已完成（§4）；**ReSTIR GI 的落点已归到 Lumen/P6**
> （`Lumen…` §5.2 任务 20：P6 = ReSTIR GI 统一估计器 + 纹理绑定数组化，验收对象是 Lumen 的多信号共存），
> 本文档只保留它与 PT 的关系；**ReSTIR PT（GRIS）为条件项**（Level 3 触发）。


### 6.1 ReSTIR GI 算法

将 ReSTIR 的 Reservoir 概念扩展到间接光照：

```
每像素 Reservoir 存储:
  - 间接光照光线 (方向 + 反弹次数)
  - 命中点的辐射度 (从 ClosestHit 返回)
  - 目标函数 (BSDF 权重)

每帧 Pass:
  1. Initial Sampling: 每像素发射 1-2 根间接光线 → 命中点 → Reservoir
  2. Temporal Reuse: 时域重投影 → 合并历史 Reservoir
  3. Spatial Reuse: 邻域空间重采样
  4. Final Shade: Reservoir 输出间接光照

与 ReSTIR DI 的区别:
  - DI: 采样光源（有限集合）
  - GI: 采样光线方向（无限连续域）→ 更难的采样问题
  - GI 需要 ClosestHit (读取命中点材质) 而 DI 只需要 shadow ray (任意命中)
```

### 6.2 ReSTIR PT (GRIS)

GRIS (Generalized Resampled Importance Sampling) 将 ReSTIR 扩展到完整路径追踪：

```
核心概念: 每个像素的 Reservoir 存储完整路径（而非单个 bounce）

Reservoir 内容:
  - 路径: [光线0, 命中0, 光线1, 命中1, 光线2, 命中2, ...]
  - 路径辐射度贡献
  - 目标函数值

优点:
  - 多个 bounce 的样本复用
  - 大光源/环境光采样质量大幅提升
  - 适用于焦散等困难光照场景

挑战:
  - 路径状态存储量大（每条路径 × N bounce 的材质数据）
  - 时域重投影验证更复杂（路径几何一致性）
```

---

## 7. 阶段 E: 硬件加速优化

> **状态（2026-09-18）：全部未做，且降级为条件触发**。实测：`DeviceCaps` 里只有 `supportsSER` /
> `supportsOMM` 两个**能力字段**（`RHI/Types.h`），RHI 未启用对应 Vulkan 扩展；`MegaGeometry`
> 全库 0 处。三者都是实时性能优化，对参考渲染器收益有限（§0.3）。


### 7.1 SER (Shader Execution Reordering)

```
问题: RT 着色器中，邻近像素的光线可能命中不同材质/几何，
    导致 GPU warp 内线程发散 (divergent execution)

SER 解决方案: 硬件重排线程执行顺序
  - SM 6.9 / D3D12_RAYTRACING_SHADER_REORDERING_TIER_1
  - VK_NV_shader_execution_reorder
  
用法 (在 ClosestHit shader 内):
  [shader("closesthit")]
  void main(inout Payload p) {
      uint materialID = g_Objects[InstanceID()].materialID;
      
      // 提示 GPU: 按 materialID 重排执行（减少材质着色器切换）
      HitObject::SetShaderExecutionReorderingHint(materialID & 0xF);
      
      // 材质着色...
  }

预期收益: RT 性能 2-3× 提升（在高度发散的场景中）
```

### 7.2 OMM (Opacity Micromaps)

```
问题: alpha-test 几何体（植被、栅栏）在 RT 中产生大量无效 ClosestHit 调用
    - ray 先命中透明像素 → 继续追踪 → 再命中不透明像素
    - 每次命中都需要执行 ClosestHit shader（即使最终会被忽略）

OMM 解决方案: 硬件级预编码透明/不透明区域
  - 离线: 对每个三角形生成 4-state 微图
    state: 0=Opaque, 1=Transparent, 2=UnknownAlpha, 3=UnknownOpaque
  - 运行时: BuildBLAS 时附加 OMM 数据
  - TraceRay: GPU 自动跳过标记为 Transparent 的微三角形
  - AnyHit shader 仅对 UnknownAlpha/UnknownOpaque 区域调用

需要的 Vulkan 扩展:
  VK_KHR_ray_tracing_opacity_micromap (或 VK_NV_ray_tracing_micromap)

预期收益: alpha-test 几何 RT 性能 2-5× 提升
```

### 7.3 RTX Mega Geometry

```
问题: 密集植被/毛发场景中，每帧更新数万个 BLAS 极慢

Mega Geometry 解决方案:
  - 分区 TLAS (Partitioned TLAS): 将 TLAS 划分为多个子树
  - Wide BVH (更宽的 BVH 节点，更高的分支因子)
  - 硬件加速的 BLAS 增量更新

适用场景: 
  - Open World 植被（Nanite 不处理植被）
  - 大规模实例化场景（数十万物体）

约束: 需要 RTX 40+ 系列 GPU
```

---

## 8. 阶段 F: 神经渲染加速

> **状态（2026-09-18）**：§8.1 STBN ✅ 已完成（2026-08-06）；§8.2 NRC / §8.3 NTC **未做**，
> 且不在本规划近期项内 —— 引擎的神经渲染能力在 AI 模块（`Engine/AI/Neural`：`NeuralUpscaler`；
> 另有 AIGC 与 GPU 推理后端），单列在 PT 规划里会与那边重复（§0.3）。


### 8.1 STBN (SpatioTemporal Blue Noise)

```
蓝噪声特性: 
  - 空间上: 低差异序列（比纯随机更均匀）
  - 时间上: 帧间无重复模式
  - 频谱上: 低频能量为 0（避免可见的结构化噪声）

使用方法:
  1. 离线生成 128×128×64 3D 蓝噪声纹理 (R8G8B8)
  2. 在 Shader 中用 SampleLevel() 采样
  3. 替代所有 Random() / Hash() 调用

示例:
  float2 blueNoiseUV = fmod(
      (float2(idx.xy) + 0.5) / 128.0  // 空间坐标
      + float2(g_FrameIndex % 8, (g_FrameIndex / 8) % 8) / 8.0,  // 时间偏移
      1.0);
  float2 xi = g_BlueNoiseTexture.SampleLevel(g_PointSampler, blueNoiseUV, 0).xy;
  // xi 替代 Random() → 更早收敛 → 更低噪声
```

### 8.2 NRC (Neural Radiance Cache)

```
目标: 用神经网络替代 DDGI 探针网格，提供更高质量的低频间接光照

方法:
  NRC 训练:
    离线: 无（NRC 是在线训练的）
  
  NRC 运行时:
    1. 每帧训练:
       a. 放置探针 (如 32×24 grid = 768 个)
       b. 对每个探针发射 1-2 根路径追踪光线 → 获取辐射度 ground truth
       c. NRC 神经网络训练 1 步（非常轻量，约 0.5ms）
       d. 更新神经网络权重

    2. 推理:
       a. 像素着色时: 查询最近 NRC 探针位置 + 视线方向
       b. NRC 推理 → 该位置的辐射度 (RGB)
       c. 作为间接光照贡献

  NVIDIA NRC SDK: https://github.com/NVIDIAGameWorks/NeuralRadianceCache

  约束: 需要 NVIDIA GPU + Cooperative Vectors (SM 6.9+) 硬件加速
```

### 8.3 NTC (Neural Texture Compression)

```
目标: 8× 纹理 VRAM 节省，同时保持高质量

方法:
  1. 离线: 训练小神经网络压缩每张纹理
     - 输入: 纹理 UV 坐标
     - 输出: RGBA 颜色
     - 网络大小: ~100KB (替代 1MB 纹理 = 10× 压缩)
  
  2. 运行时:
     - RT 着色器中: 在 ClosestHit 时调用 NTC 推理 → 获取纹理颜色
     - 或: 通过 Cooperative Vectors 硬件加速推理

  约束: 需要 RTX 40+ GPU + Cooperative Vectors 支持
```

---

## 9. 阶段 G: 完整材质系统

> **状态（2026-09-18）**：前提已部分具备 —— `pbr_common.slang` 的 `PBR_BRDF(..., disneyA, disneyB)`
> **已经支持** anisotropic / subsurface / specular / sheen / clearcoat / clearcoatGloss / specularTint。
> PT 侧缺的是**采样端**（`PT_Common.SampleBSDF` 只有 metallic/roughness 两瓣）与 **`PathPayload`
> 的字段**（48B，无 disney/IOR/transmission）。因此本阶段的优先项不是"重新实现各 lobe"，
> 而是 **① 扩展 PathPayload ② PT 采样端接入共享 BRDF**（§0.4 第 1 项）。


### 9.1 glTF PBR 扩展支持

在光栅化路径中已支持的扩展需要在 PT 路径中重新实现：

| 扩展 | PT 中的实现 | 位置 |
|------|-----------|------|
| **Transmission** | 透明/折射: BSDF 采样时，以 IOR 决定折射方向 → Refraction Ray | `SampleBSDF()` 中分支 |
| **Volume** | 参与介质: Beer's Law 衰减, 随机弹射距离采样 | `PathPayload` 中加 volumeDistance |
| **Clearcoat** | 双层 BRDF: 层1 (Coat) → 层2 (Base), NDF 混合 | `EvaluateBRDF()` 中加 clearcoat 项 |
| **Sheen** | 织物微表面: Charlie/Ashikhmin NDF | `EvaluateBRDF()` 中加 sheen 项 |
| **Anisotropy** | 各向异性 GGX: 双粗糙度 (roughnessX, roughnessY) | `D_GGX_Aniso()` |
| **Iridescence** | 薄膜干涉: 波长相关 IOR → 光谱渲染 | 需要 Spectral Rendering 修改 |
| **Specular** | 完整 Fresnel: 有色 F0 + IOR 控制 | 替换 Schlick 为 Exact Fresnel |
| **Dispersion** | 色散: 波长相关折射 → 光谱分离 | 需要 Spectral Rendering 修改 |

### 9.2 BSDF 采样统一接口

```hlsl
// pbr_common.slang — 统一 BSDF 采样 (光栅化 + RT + PT 共用)

struct BSDFSample {
    float3 wi;          // 采样方向
    float3 f;           // BSDF 值 (RGB)
    float  pdf;         // 概率密度
    uint   eventType;   // 0=Diffuse, 1=Specular, 2=Transmission, 3=Volume
};

BSDFSample SampleBSDF(
    float3 N, float3 V, MaterialData mat,
    float2 xi,           // 随机数 (蓝噪声)
    float3 T,            // 切线 (各向异性用)
    float3 B)            // 副切线
{
    float metallic = mat.metallic;
    float roughness = mat.roughness;
    float transmission = mat.transmission;
    float ior = mat.ior;
    
    // ... 完整 BSDF 采样逻辑 ...
    // 根据材质参数决定采样策略 (Diffuse lobe / Specular lobe / Transmission)
}
```

---

## 10. 实施时间线

> **状态（2026-09-18）：本节时间线已失效**，仅作历史记录。实际：A+B 约 1 周（2026-07-27 立项 →
> 08-04 完成），三件套 2 天（08-06）。剩余工作按 §0.4 的清单与 §0.3 的条件触发项管理，不再按周排期。


### 10.1 总览

```
Phase PT-A: PT 参考模式       ████████████  (8 周)
Phase PT-B: ReSTIR DI         ██████        (4 周)
Phase PT-C: NRD 降噪器        ██████        (4 周)
Phase PT-D: ReSTIR GI + PT    ██████████    (6 周)
Phase PT-E: 硬件加速          ████          (3 周)
Phase PT-F: 神经渲染          ██████        (4 周)
Phase PT-G: 完整材质          ██████        (4 周)
                               ─────────────────────
                               总计 ~33 周 (约 8 月)
```

### 10.2 详细时间线

```
W1-W3:   PT 参考 RayGen + ClosestHit 着色器
         NEE + MIS + 俄罗斯轮盘赌
         基础天空光照

W4-W5:   PT 参考模式 RenderGraph
         与现有 DeferredPipeline 共存（r.Pipeline.Mode CVar 切换）
         ImGui 质量面板 (SPP, bounces, 降噪开关)

W6-W7:   多采样 + 时域累积
         蓝噪声采样 (STBN 第一阶段)
         性能基准测试

W8:      PT 参考模式发布 + 文档

--- Phase B (W9-W12) ---

W9-W10:  ReSTIR DI Initial Sampling + Reservoir
W11:     ReSTIR DI Temporal + Spatial Reuse
W12:     ReSTIR DI 集成测试 + 性能对比

--- Phase C (W13-W16) ---

W13-W14: NRD 集成 (ReBLUR + SIGMA)
W15:     NRD ReLAX (ReSTIR DI 信号)
W16:     降噪质量对比 (NRD vs 自研 TAA)

--- Phase D (W17-W22) ---

W17-W18: ReSTIR GI (间接光照 Reservoir)
W19-W20: ReSTIR PT (GRIS 路径追踪重采样)
W21-W22: 集成测试 + 调参

--- Phase E (W23-W25) ---

W23:     SER 集成 + 测试
W24:     OMM 集成 (alpha-test 几何)
W25:     Mega Geometry (密集植被)

--- Phase F (W26-W29) ---

W26-W27: NRC 集成 (替代 DDGI 探针)
W28:     STBN 第二阶段 (优化样本质量)
W29:     NTC 纹理压缩 (可选)

--- Phase G (W30-W33) ---

W30-W31: 完整 glTF 材质扩展 (transmission/volume/clearcoat/sheen/anisotropy)
W32-W33: 全场景测试 + 性能优化 + 文档
```

---

## 11. 性能分析

> **状态（2026-09-18）**：本节按"实时全 PT"设定的帧率目标不再适用（PT 是参考渲染器）。
> 需要的是**参考模式的可用性**：能在合理等待内收敛、结果可复现（用于对照近似 GI）。
> 已知约束：本机 Intel Arc 上 RT 管线编译约 20s/个、02.Cube 启动 80~110s（附录 C 已知问题 1）。


### 11.1 目标性能 (RTX 4070 @ 1440p)

| 阶段 | 帧率 | 帧时间 | 主要优化手段 |
|------|:---:|:---:|------|
| PT 参考 (朴素) | 5-10 | 100-200ms | 无优化，最大质量 |
| PT 参考 + 1spp 降噪 | 15-20 | 50-66ms | NRD + TAA |
| +DLSS Performance (50%) | 30-40 | 25-33ms | 内部分辨率 720p |
| +ReSTIR DI | 40-50 | 20-25ms | 时空复用减少 shadow ray |
| +ReSTIR GI + DLSS-FG | 60-80 | 12-16ms | Frame Generation |
| Level 3 全优化 | 60-100 | 10-16ms | SER + OMM + NRC + DLSS-RR + MFG |

### 11.2 关键性能拐点

```
最大性能增益单点:
  1. DLSS Super Resolution (2-3×): 降低渲染分辨率
  2. ReSTIR DI (5-10× 有效样本): 减少 shadow ray SPP
  3. SER (2-3×): 减少 divergent execution
  4. NRC (3-5× GI): 替换路径追踪 GI
  5. DLSS Frame Generation (2×): 帧率翻倍
  6. DLSS Ray Reconstruction (去噪质量): 替代多 SPP 降噪

综合:
  10fps × 2(DLSS) × 2(ReSTIR+NRD) × 2(SER+NRC) × 2(FG) = 80fps
```

---

## 12. 后续任务清单（从 1 重新计数，2026-09-18）

> 本节把 §0 的复核结论落成**可执行清单**。编号从 **1** 连续编号，与 §1~§11 的"阶段"字母编号
> **互不相干**（阶段字母作为别名保留，便于对照历史设计）。
>
> **怎么用这张表**：先做 **A 组（1~6）** —— 它们没有外部依赖，且都在"参考渲染器"这个当前定位之内；
> **B 组（7~14）是触发式**，触发条件写在表里，**条件未到之前不做**（提前做属于"没有消费方的泛化"，
> 与 GI/Lumen 那边的取舍一致）；**C 组（15~16）** 需要另一份规划先到位。每完成一项：状态改 ✅
> 并补提交号，**不要改动已有编号**（编号不复用）。
>
> 建议顺序：**1 → 2 → 3 → 5 → 6 → 4**（材质链 1→2→3→4 需要连着做；工具链 5→6 可并行推进）。
>
> **载体示例（2026-09-18 新增）**：§12 全部 PT 任务的冒烟载体是 `Samples/05.Sponza-PathTracing`
> —— 与 03/04 相同的 Sponza glTF 场景（含 skybox / 方向光 / 点光 / 聚光），渲染改走
> `PathTracingPipeline`（完全不做光栅化）。ImGui 面板暴露本清单涉及的开关与参数：
> SPP / 弹射次数 / 天空强度 / 时域降噪 / A-Trous / ReSTIR DI / NEE MIS / 俄罗斯轮盘赌，
> 参数在退出时写回 `Content/Config/05_Sponza-PathTracing.cfg`。**每个任务完成后都要在该示例上
> 跑通（Release 冒烟 + 日志无 `error` / `VUID`）再提交**，保证"文档中的功能"都有可复现的验证入口。

### A. 参考渲染器定位下的近期任务（无外部依赖）

| 编号 | 任务 | 类型 | 依赖 | 判据要点 |
|:---:|---|---|---|---|
| **1** ✅ | **`PathPayload` 扩展**：48B（albedoMetallic + normalRough + emissive）→ 带上 `disneyA` / `disneyB`，并按需预留 IOR / transmission 字段位 | 功能 | 无 | 结构与 Slang cbuffer/struct 两端**逐字段偏移一致**（`static_assert` + offsetof 断言）；`PT_Full.rchit` 写入新字段；默认参数下现有白炉读数与对照读数**不变**（等价性） |
| **2** ✅ | **PT 采样端接入共享 BRDF**：让 `PT_Common` 的求值用 `pbr_common.PBR_BRDF(..., disneyA, disneyB)`，而不是另写一套 lobe | 功能 | 1 | 同一材质、同一组方向对，PT 与光栅化路径的 BRDF 求值一致（CPU 复算对照）；白炉能量守恒不破；单测全绿 |
| **3** ✅ | **clearcoat / sheen / anisotropy 参与采样**（当前 `SampleBSDF` 只有"余弦 + GGX"两瓣） | 功能 | 1、2 | 每瓣的采样 pdf 与求值一致（MIS 权重可算、无偏）；把该 lobe 权重置 0 时与任务 2 逐像素一致 |
| **4** ✅ | **transmission / volume**：折射（IOR）+ 参与介质（Beer's Law + 随机距离采样） | 功能 | 1、2 | 折射方向与菲涅尔分配符合 §9.1 设计；均匀介质的衰减与解析解对照；场景默认（无 transmission）时读数不变 |
| **5** ✅ | **PT 对照流程脚本化**：把"PT 当标准答案"的流程做成一键可跑（同一 cfg 的**私有副本** → `HE_DUMP_GI` 落盘 → 复用 `Tools/gi/*` 检查脚本出读数） | 工具 | 无 | 一条命令跑完全流程；连续 N 次运行读数离散 < 阈值（可复现）；文档写清用法与注意事项（cfg 会被示例回写，禁止复用同一文件） |
| **6** ✅ | **参考模式的"收敛"判据与对照实验模板** | 文档 + 工具 | 5 | 明确定义收敛（SPP / 时间 / 误差阈值）；给出至少一组真实对照（PT vs 某个近似 GI 的偏差读数与结论） |

> **A 组完成记录**
>
> · **1 ✅ 2026-09-19（提交 `79a6c61`）**：`PathPayload` 48B → **96B**（3×float4 命中信息 + `disneyA` / `disneyB` / `surfaceParams`），
>   `surfaceParams = (disneyC, dielectricF0, ior, transmission)`，其中 transmission 为**预留字段位**（任务 4 才参与折射）。
>   数据链：glTF `KHR_materials_*` → `MeshComponent` 新字段 → `RTPass` 材质纹理 row4~row6 → `PT_Full.rchit` 写入载荷；
>   两端布局由 C++ 侧 `static_assert`+`offsetof` 与 `Tools/check_path_payload.py` 双保险（Slang 编译器不支持 `sizeof` 形式的静态断言，实测报 E20001）。
>   验证：`HugEngineTests` 203 用例 / 5515 断言全绿（新增 `TestPathPayload.cpp` 3 用例）；`05.Sponza-PathTracing` Release 冒烟 150s 无 `[error]`，
>   校验层 VUID 种类与数量与改动前**逐项一致**（等价性：rgen 采样/求值路径未改，只有 rchit 多写载荷字段）。
>
> · **2 ✅ 2026-09-19（提交 `444cac9`）**：求值端不再吃 `PBR_BRDF` 的默认实参 —— `PT_Full.rgen` 的三处调用
>   （ReSTIR 分支 / NEE 循环 / 路径吞吐更新）显式传入载荷里的 `dielectricF0` / `disneyA` / `disneyB` / `disneyC`，
>   IBL 镜面 F0 也从硬编码 `0.04` 换成材质的 `dielectricF0`（默认材质下数值等价）。
>   参数打包收敛为**唯一实现** `Engine/Render/RT/PTMaterialParams.h`：光栅化 CPU 侧（`Material.h` 的 `GPUObjectData` /
>   `GPUMaterialData`）与 RT 材质纹理 row4~row6（`RTPass`）都调用它，默认值与 `PBR_BRDF` 的默认实参逐字段相同，
>   因此「PT 与光栅化对同一材质算出不同 BRDF」在结构上不可能发生。
>   验证：`HugEngineTests` 206 用例全绿（新增 `TestPTMaterialParams.cpp` 3 用例：默认等价 / IOR→F0 推导 / 与 `PathPayload`
>   默认字段一致）；`05.Sponza-PathTracing` Release 冒烟无 `[error]`、VUID 集与任务 1 后逐项一致。
>   **已知限制**：ReSTIR DI 的三个 compute pass 仍用默认 Disney 参数（它们没有材质纹理绑定，PT 的 5 张 UAV 无空闲分量
>   可携带 disney）；Sponza 这类无扩展材质下与 PT 等价，带 clearcoat/sheen 等扩展材质时存在 RIS 目标分布偏差，
>   待 ReSTIR 进入实际使用（B 组触发）或统一采样端时补数据通路。
>
> · **3 ✅ 2026-09-19（提交 `bb2d884`）**：采样端由「余弦 + 各向同性 GGX」两瓣重构为**三瓣混合 + 统一 pdf**：
>   新增 `PTBSDFParams` / `MakeBSDFParams`（求值、采样、pdf 共用同一份参数）、`BSDFTangentBasis`（与 `PBR_BRDF` 内
>   的切线基逐行一致）、`BSDFLobeProbabilities`（漫反射瓣含 sheen/subsurface、基础镜面瓣、清漆瓣）、
>   `SampleGGXHalfAniso`（各向异性 GGX 半向量采样，方向相关 alpha）、`BSDFPdf`（三瓣混合 pdf）、
>   三瓣版 `SampleBSDF`，以及统一求值入口 `PT_EvalBRDF`。
>   MIS 权重改为直接调用采样端的 `BSDFPdf`（原先是在 NEE 里内联复制一份两瓣公式，容易与采样端漂移）。
>
>   **默认材质的代数等价性**（Sponza 全部材质 clearcoat=0 / sheen=0 / aniso=0）：`pClearcoat=0`、`base=1` ⇒
>   `pSpec=lerp(0.35,0.95,metallic)`、`pDiff=1-pSpec` 与旧实现同式；采样分支 `uLobe.x<pDiff` 走余弦、否则走
>   `SampleGGXHalf(N, roughness, …)`（aniso=0 时不走各向异性路径）与旧实现同函数；pdf 组成
>   `pDiff·(NdotL/π) + pSpec·(D·NdotH/4NdotV)` 与旧内联公式逐项相同 ⇒ 与任务 2 的输出逐像素一致。
>   验证：shader 经 slangc 通过（含 `D_GGX_aniso` 路径）；`05.Sponza-PathTracing` Release 冒烟无 `[error]`、
>   校验层 VUID 集与任务 2 后逐项一致。
>   **待补**：判据里的「逐像素对照」目前只有代数等价性 + 运行期无回归两类证据，真正的逐像素落盘对照需要
>   `HE_DUMP_PT` 落盘流程 —— 已列为**任务 5 的验收项**（届时对任务 2/3 两版 exe 各跑一次 dump 做逐像素差值）。
>   **已知近似**：各向异性 lobe 用的是「方向相关 alpha」的 NDF 采样（非 VNDF，与 `D_GGX_aniso` 配对使用），
>   默认材质不涉及；sheen 的掠射边缘项归入漫反射瓣（余弦采样），pdf 与采样策略一致，故估计仍无偏。
>
> · **5 ✅ 2026-09-19（提交 `7f5a097`）**：PT 参考图落盘 + 一条命令跑完的对照流程。
>   · 示例侧：`05.Sponza-PathTracing` 支持 `HE_DUMP_PT=<标签>` / `HE_DUMP_PT_FRAME=<帧号，默认 60>`，
>     把 PT 的 4 张输出（hdr / depth / normal / albedo，未 ToneMap 的线性辐射度）整幅拷回 host 落盘为
>     `build/verify/pt_<标签>_<目标>.f16`（原始像素、无文件头、行紧密排布）+ `_meta.txt` + `_camera.txt`，
>     落盘后自动退出；`PTPass` 的输出纹理因此补了 `TransferSrc` 用途。
>   · 工具侧：`Tools/pt/dump_pt.ps1`（跑 N 次 + stale 守卫 + 调分析）、`Tools/pt/analyze_pt.py`
>     （统计 + 可复现性离散度 + `--diff` 逐像素对照）。
>   · **用法**：`powershell -ExecutionPolicy Bypass -File Tools/pt/dump_pt.ps1 -Tag base -Runs 3 -Frame 120`；
>     两版对照：`python Tools/pt/analyze_pt.py --diff <tagA> <tagB>`。
>   · **注意事项**（判据要求写清）：① 示例退出时会回写 cfg，脚本**每次运行都用私有副本**
>     （`HE_CFG` 覆盖路径）——禁止多次运行复用同一个 cfg，否则前一次的读数会污染下一次的基准；
>     ② stale-dump 守卫：运行前清旧产物、运行后要求产物比启动时间新，避免把上一次的结果当成本次读数；
>     ③ 采样帧号决定时域累积程度（降噪 + A-Trous 有历史），不同帧号之间不可直接比，同一帧号下
>     同机同驱动是**确定性**的（随机数来自 STBN 的 (像素, 帧号)）。
>   · 读数（本机 RTX 4060 Laptop，帧 60，2 次运行）：`mean=0.281079 p50=0.268921 p95=0.272257 max=527.333313`，
>     两次运行的**逐像素最大绝对差 = 0**，离散度 0 < 阈值 1e-4 ⇒ 可复现。
>   · 不沿用 `Tools/gi/*`：那套脚本的目标集合与命名（`gi_<tag>_*`、Provider/GBuffer 语义）面向 GI，
>     PT 的目标与格式不同；新脚本沿用同一套落盘约定（无头 .f16 + `_meta.txt` + stale 守卫），避免把两套语义揉在一起。
>
> · **3 的逐像素对照（回溯补做，任务 5 的验收项）**：把 shader 临时回退到任务 2 的版本各构建一个 exe，
>   同帧同参各跑一次 dump 后 `--diff`：**depth / normal / albedo 逐像素差为 0（各 2073600 / 518400 元素），hdr 仅 6/2073600 个元素有差异（最大绝对差 4.88e-04、最大相对差 8.92e-04、RMSE 5.87e-07、超容差元素 0），来源是 pdf 计算由分支内联改为函数调用后的浮点结合顺序，非算法差异** ⇒ 任务 3 的「lobe 权重置 0 时与任务 2 逐像素一致」成立。
>
> · **6 ✅ 2026-09-19（提交 `fa93326`）**：收敛判据 + 对照实验模板（全部读数由任务 5 的落盘流程产生，可复现）。
>
> **① 收敛的定义**（工程判据，工具里就是这条）：
> · **主判据（鲁棒统计量）**：以亮度 P50（图像中心趋势）与 P95（高频尾部）为准，相邻档位（时间维 = 采样帧号；
>   SPP 维 = `r.PT.SPP`）的相对变化 **< 1%** 即认为后一档已收敛；
> · **`mean` 只作辅助**：它被极少数超亮像素拖动（实测 `max` 在 317 ~ 9969 之间跳），拿它当主判据会把
>   「只有几个像素在抖」误判成「整幅没收敛」。工具因此同时打印 `max` 以便识别这种情形；
> · 判据由 `python Tools/pt/analyze_pt.py --converge <tag...> --conv-tol 0.01` 自动判定。
>
> **② 时间维读数**（Sponza，PT 默认参数，帧 30 / 60 / 120 / 240，同一相机）：
> `p50 = 0.268921`（四档**完全一致**，变化 0.000%）；`p95 = 0.272257 → 0.272217`（≤ 0.015%）；
> `mean = 0.284434 / 0.281079 / 0.277662 / 0.336982`（变化 1.18% / 1.22% / 21.4%）；`max = 785 / 527 / 317 / 9969`。
> ⇒ **主体在帧 30 即收敛**；`mean` 的抖动完全来自极少数像素的尖峰（`max` 同步跳变），不是整幅不收敛。
>
> **③ SPP 维读数**（同帧号 120，`pt_spp` = 1 / 2 / 4 / 8）：
> `p50 = 0.268921 / 0.268962 / 0.269084 / 0.269328`（累计 +0.15%，相邻 ≤ 0.091%）；
> `p95 = 0.272257 / 0.274170 / 0.282511 / 0.288005`（相邻 0.70% / 3.04% / 1.94%）；
> `mean = 0.277662 / 0.361741 / 0.335005 / 0.389063`；`max = 317 / 4791 / 5952 / 6760`。
> ⇒ **中心趋势（p50）对 SPP 不敏感，高频尾部（p95）到 SPP 8 仍未进 1%**：低 SPP 会系统性漏掉稀疏的高贡献路径
>   （天空/镜面亮点），因此**报读数必须同时声明 SPP、帧号与所依据的分位数**；需要尾部可信时用 SPP ≥ 8 并
>   在结论里注明仍未达 1%。
>
> **④ 真实对照：PT（标准答案）vs Deferred 的 GI 层栈**（同一 Sponza、同一相机、同帧 120；
>   `HE_DUMP_MODE=deferred` 改走延迟管线，其余参数与环境完全一致）：
> | 读数 | PT（标准答案） | Deferred（GI 层栈） | 相对偏差 |
> |---|---:|---:|---:|
> | 亮度 mean | 0.277662 | 0.413351 | **+48.87%** |
> | 亮度 P50 | 0.268921 | 0.270040 | +0.42% |
> | 亮度 P95 | 0.272257 | 0.777262 | **+185.49%** |
> | 亮度 max | 317.25 | 363.19 | — |
>
> 逐像素（亮度）：平均带符号偏差 **+0.135690**；绝对偏差 P50 = 0.076274 / P95 = 0.505534 / max = 362.92；
> 相对偏差 P50 = 28.20% / P95 = 186.39%。按基准亮度分档：`[0.10, 1.00)` 的 518316 个像素（99.98%）
> 平均相对偏差 **+52.01%**，`[1.00, ∞)` 的 84 个像素 **−85.98%**。
> **结论**：延迟管线的 GI 层栈在**中间调与亮部整体偏亮约 50%**、尾部（P95）偏亮 185%，而最暗的中心趋势
> 几乎一致（P50 +0.42%）—— 这不是"整体曝光差"，而是**亮部能量偏高**，方向指向层栈里 IBL 与探针/屏幕空间
> 源的叠加或归一化；该偏差由 PT 标准答案定量给出，交由 GI 侧复核（本规划不在此处改动 GI 实现）。
>
> **⚠ 2026-09-19 更正**：上表是**修复前**的读数 —— 当时 PT 受 §0.6 的两处缺陷影响呈"平场纯金属"，
> 与 Deferred 的差异被严重低估。修复后同配置（默认 cfg、帧 120）重测：
> PT `mean=0.176080 p50=0.182983 p95=0.348145 max=31.46`，
> Deferred `mean=1.129088 p50=1.216797 p95=2.143880 max=16.35`；
> 偏差为 **mean +541.24% / P50 +564.98% / P95 +515.80%**（按基准亮度分档：`[0,0.1)` +474.50%、
> `[0.1,1.0)` +562.46%、`[1.0,∞)` −57.39%）。结论方向不变（GI 层栈偏亮），但量级从"约 +50%"变为
> **约 6 倍**，需按新读数在 GI 侧复核；`python Tools/pt/analyze_pt.py --compare <pt> <deferred>` 可复现。
>
> **⑤ 对照实验模板**（一条命令一族，全部落盘可复现）：
> ```powershell
> # 参考图（标准答案）
> powershell -ExecutionPolicy Bypass -File Tools/pt/dump_pt.ps1 -Tag pt   -Runs 1 -Frame 120 -Mode pt
> # 待测管线（GI 层栈）
> powershell -ExecutionPolicy Bypass -File Tools/pt/dump_pt.ps1 -Tag df   -Runs 1 -Frame 120 -Mode deferred
> # 读数对照
> python Tools/pt/analyze_pt.py --compare pt_run1 df_run1 --target hdr
> # 收敛曲线（时间维 / SPP 维）
> python Tools/pt/analyze_pt.py --converge conv30_run1 conv60_run1 conv120_run1 conv240_run1 --conv-tol 0.01
> python Tools/pt/analyze_pt.py --converge spp1_run1 spp2_run1 spp4_run1 spp8_run1 --conv-tol 0.01
> ```
> 任意参数覆盖走私有 cfg 副本：`-Spp 4`（等价 `pt_spp=4`）或 `-Set pt_bounces=8`；
> 注意 `-Set` 只写进程的副本，基准 cfg 不受影响（任务 5 的注意事项）。
>
> · **4 ✅ 2026-09-19（提交 `99540e9`）**：transmission / volume 落地（A 组最后一项）。
>   · **折射**：`SampleBSDF` 新增透射 delta 瓣 —— 分支概率按**能量份额**分配
>     （`pRefl = t·F`、`pRefr = t·(1-F)`、`pBsdf = 1-t`，t = transmission），菲涅尔用 Schlick、
>     折射方向用 Snell（`RefractDirection`，含全内反射判定 → 退化为反射）。由于分支概率与 f 中的
>     份额一致，delta 分支权重恒为 1（反射）或 baseColor（折射），连续 BSDF 分支照常 `f/pdf`
>     （`(1-t)` 在 f 与 pdf 中相互抵消）⇒ 整条路径的估计保持无偏；NEE 的 MIS pdf 折算 `(1-t)`
>     以匹配"含界面分支"的采样策略。
>   · **介质**：`PathPayload` 96→112B（新增 `volumeParams = σ_t.rgb`），材质纹理 7→8 行（row7）；
>     `σ_t = -ln(attenuationColor)/attenuationDistance`；折射进入介质后按 **Beer-Lambert 解析解**
>     衰减 `exp(-σ_t·d)` —— 这正是 §9.1 要求的"随机弹射距离采样"的期望值（用期望值代替采样可显著
>     降方差且保持无偏）；`SampleDistanceInMedium` 一并提供，供将来实现介质内散射时使用。
>   · **验证**：① 判据「场景默认（无 transmission）时读数不变」—— 与任务 6 的基线 dump（帧 120）
>     逐像素对照，hdr / depth / normal / albedo 四个目标的 **RMSE = 0、超容差元素 0**；
>     ② 判据「均匀介质衰减与解析解对照」—— `TestPTVolumeMath.cpp` 用 20 万分层样本验证生存概率
>     == `exp(-σ·D)`、自由程均值 == 1/σ、中位自由程 == ln2/σ，并覆盖 Snell 定律（`sinθt = η·sinθi`）、
>     临界角两侧的全内反射、Fresnel 两端点、σ_t 推导；③ `HugEngineTests` **211 用例 / 5570 断言全绿**；
>     ④ 常规冒烟无 `[error]`，校验层 VUID 集 11 种 / 78 条与之前一致。
>   · **已知近似（如实记录）**：只做**吸收**不做介质内散射（无次表面弹射，故按"有色玻璃"语义）；
>     折射只处理"一次界面穿越"、不追踪介质进出状态（嵌套 volume 场景需要更完整的介质栈，属后续项）；
>     iridescence / dispersion 仍因需要光谱渲染而排除（§9.1 原表已注明）。


### B. 条件触发（Level 3 实时化；条件未到不做）

| 编号 | 任务 | 触发条件 | 备注 |
|:---:|---|---|---|
| **7** | ReSTIR PT / GRIS 路径重采样 | 决定把 PT 转为**实时主路径** | 设计见 §6.2；参考渲染器靠多 SPP 收敛即可 |
| **8** | SER（Shader Execution Reordering） | 同上 + RHI 启用 `VK_NV_ray_tracing_invocation_reorder` | 现状只有 `DeviceCaps::supportsSER` 字段，未启用扩展 |
| **9** | OMM（Opacity Micromaps） | 实时 PT 且场景存在 alpha-test 几何 | 与 `Nanite_N1至N3实现计划.md` 有重叠，需先定落点 |
| **10** | RTX Mega Geometry | 实时 PT + 集群 LOD 几何 | 只需要**厂商 Vulkan 扩展**（`VK_NV_ray_tracing_mega_geometry`），**不引入 SDK**；引擎当前未启用任何 `VK_NV_*` 扩展 |
| **11** | NRC（神经辐射缓存） | 实时 PT；落点可能在 AI 模块 | **不引入外部 SDK**：运行时可用引擎自研 GPU 推理后端（`Engine/AI/Runtime`：`AIDevice` / `GPUBackend` / `InferenceScheduler`），训练在离线设施完成；见 §8.2 |
| **14** | 时间线 / 性能目标重排 | Level 3 正式立项 | §10 / §11 在立项时重写（当前已标注失效） |

> **2026-09-18 删除记录（不引入外部 SDK）**：原 **12（NTC 神经纹理压缩）** 与
> **13（DLSS Ray Reconstruction）** 已从本清单删除 —— 理由是**不让引擎依赖外部 SDK**：
> NTC 需要 NVIDIA **libntc**（CUDA 编解码库），DLSS-RR 需要 **Streamline + NGX**（闭源驱动组件）。
> 在"PT = 参考渲染器"的定位下，这两项都不值得背负这类依赖。
> · **编号 12 / 13 不复用**：后续新任务从 **17** 起编号（14~16 已在用）；
> · 两项的原始设计仍保留在 §5.4 / §8.3，作为资料而非计划任务；
> · 同组保留的 10 / 11 各有说明：10 只需厂商扩展、11 走自研推理后端，均**不引入 SDK**。

### C. 与其它规划的接口项（需要那边先到位）

| 编号 | 任务 | 需先到位 |
|:---:|---|---|
| **15** | 统一降噪框架 **11.3**（按信号分派 + `alpha < 0` 有效性契约），并把 PT 的"时域累积 + A-Trous"注册为框架内的一个信号 | `Lumen与Nanite完整设计规范` §5.1（11.1 / 11.2 已完成，11.3 待做） |
| **16** | **P6**（ReSTIR GI 统一估计器 + 纹理绑定数组化）与 PT 估计器的关系澄清：PT 是复用该估计器还是保持独立 | `Lumen…` §5.2 任务 20（长期项，未开始） |

---

## 附录 A: 与混合 RT 管线的依赖关系（2026-09-18 核对）

> **前置关系已变化**：`HybridRTPipeline` 已删除、RT 效果已并入 Deferred 层栈
> （`Engine/Render/GI/GITypes.h` 注明"光追已并入 Deferred（HybridRT 管线已移除）"）。
> 原《混合RayTracing管线规划》保留在 `docs/已实现功能/`，作为 RT 基础设施的设计来源。

| 原列前置项 | 现状 |
|---|:---:|
| RHI RT 接口完整 | ✅ |
| TLAS/BLAS 构建/更新 | ✅ |
| SSBO 在 ClosestHit 中的修复 | ✅（`RT_HitCommon` / `PT_Full.rchit` 各自读取，见 shader 注释） |
| RTManager 重构 | 🔶 **未按原方案重构**：改为 `RTPass` + `PathTracingPipeline::SetSharedRTPass` 复用同一份加速结构 |
| 共用 `pbr_common.slang` | ✅ 光栅化 / RT 侧共用；**PT 采样端尚未接入**（§0.3、§9） |
| STBN 蓝噪声纹理 | ✅ 2026-08-06 |

下列项**既不在混合 RT 阶段、也不在当前 PT 目标内**（见 §0）：ReSTIR GI/PT、NRD 集成、
SER/OMM、NRC、DLSS-RR。

## 附录 B: 参考资源

| 技术 | 文献 |
|------|------|
| ReSTIR DI | "Spatiotemporal reservoir resampling for real-time ray tracing with dynamic direct lighting" (SIGGRAPH 2020) |
| ReSTIR GI | "Generalized Resampled Importance Sampling: Foundations of ReSTIR" (SIGGRAPH 2022) |
| ReSTIR PT | "ReSTIR Path Tracing: A Practical Approach" (SIGGRAPH 2025) |
| NRD | NVIDIA RTX Kit: NRD Documentation |
| DLSS-RR | "Ray Reconstruction: Transformer-based Denoising for Path Tracing" (SIGGRAPH 2023) |
| NRC | "Real-time Neural Radiance Caching for Path Tracing" (SIGGRAPH 2021) |
| SER | NVIDIA Ada Whitepaper (2022) |
| OMM | NVIDIA Blackwell Whitepaper (2024) |
| STBN | "Spatiotemporal Blue Noise Textures" (EGSR 2022) |
| RTXDI | NVIDIA RTXDI SDK Documentation + Sample Code |

---

> **文档版本**: v1.4
> **创建日期**: 2026-07-27
> **更新**: 2026-09-18 — §12 清单按"**不引入外部 SDK**"筛选：删除 **12（NTC，依赖 libntc）** 与
>   **13（DLSS Ray Reconstruction，依赖 Streamline + NGX）**；编号不复用（后续新任务从 17 起）
> **更新**: 2026-09-18 — 新增 **§12 后续任务清单（从 1 重新计数）**：A 组近期 6 项（参考渲染器定位内）、
>   B 组条件触发（Level 3 实时化）、C 组跨规划接口 2 项
> **更新**: 2026-09-18 — **定位修订 + 现状复核**：PT 当前的定位是「参考渲染器 / Ground Truth」
>   （不是实时管线），Level 3 实时化路线（NRD / SER / OMM / Mega Geometry / NRC / NTC / DLSS-RR /
>   33 周时间线 / 实时帧率目标）改为**条件触发项**；新增 **§0 现状复核（2026-09-18）**，
>   各阶段就地标注状态，§4.2 的 ReSTIR 最终着色公式就地修正，附录 A 更新依赖现状
> **更新**: 2026-08-06 — 质量提升三件套（albedo UAV / STBN / A-Trous）已完成
> **更新**: 2026-08-04 — 阶段 A（PT 参考模式）+ 阶段 B（ReSTIR DI）已完成
> **工期**: 原计划 33 周（按"实时全 PT"排期，**已失效**，见 §0.3）
> **前置条件**: [混合 Ray Tracing 管线](../已实现功能/混合RayTracing管线规划.md) —— 已完成，且 RT 效果已并入 Deferred 层栈（HybridRTPipeline 已删除）

---

## 附录 C: 实施进度（2026-08-04）

### 已完成

| 阶段 | 状态 | 内容 |
|------|:---:|------|
| 阶段 A: PT 参考模式 | ✅ | `PathTracingPipeline`（r.Pipeline.Mode=3）：迭代式路径追踪 RayGen（NEE+MIS+俄罗斯轮盘赌+天空）+ PT_Full.rgen/rchit/rmiss + PTPass（4 输出 UAV：HDR/深度/法线+粗糙度/速度）+ RTDenoiser 时域累积 + ToneMap/FXAA + ImGui 质量面板 |
| 阶段 B: ReSTIR DI | ✅ | `ReSTIRPass`（Init→Temporal→Spatial 三个 compute 单 RG Pass 顺序执行）+ 蓄水池 SSBO 双缓冲 + 历史纹理双缓冲 + PT_RayGen 反弹 0 读 FinalReservoir 替代 NEE（r.PT.ReSTIR 开关，默认关闭） |
| 材质纹理 4 行扩展 | ✅ | `BuildSceneMaterialTexture` 3×N → 4×N（row3=emissive.rgb）；`LoadHitMaterial` 增加 emissive 输出 |

### 质量提升三件套（2026-08-06 完成）

在阶段 A/B 基础上补齐三项质量改进（commit `fa2d287..68d3ea3`，均本机 Intel Arc 可验证）：

| 项目 | 状态 | 内容 |
|------|:---:|------|
| PT albedo 第 5 UAV + ReSTIR 精化 | ✅ | `PTPass` 新增第 5 输出 UAV `GetAlbedoMetallic()`（RGBA16_FLOAT，rgb=albedo a=metallic，set0 b9）；三个 ReSTIR compute 的 `EvaluateTarget` 用真实 albedo/metallic（Init b4 / Temporal b11 / Spatial b5）；`ptAlbedo` 进 RenderGraph 依赖两端（PT_Render UAV 写 + ReSTIR_DI read）补齐 UAV→SRV 屏障 |
| STBN 时空蓝噪声 | ✅ | `Tools/gen_stbn.py` 离线生成（Mitchell 最佳候选采样 → 环面距离场 → 直方图均衡化，128×128×64 RGBA8，一次性运行 ~22s）→ `STBNData.h` 内嵌 4MB → `STBNTexture` 3D 纹理（depth=64, initialData）→ `PT_Common` 的 `Rand/RandInt/StratifiedJitter` 改走 `g_BlueNoise.Load()`（签名不变，调用点零改动）；**STBN 初始化失败 → 禁用 RT**（shader 无 Hash 回退，避免读未绑定描述符） |
| A-Trous 空间滤波 | ✅ | `PTAtrousPass`（compute）+ `PT_Atrous.comp.slang`：SVGF 风格 4 迭代（step 1,2,4,8）5-tap 十字核，depth/normal/luminance 边权重 + 3×3 局部亮度方差自适应 σ + 可选火萤钳制；帧图插在 PT_Denoise 之后，ToneMap 输入三级优先（A-Trous→降噪→原始 HDR）；`r.PT.Atrous*` 6 个 CVar + ImGui 面板控件 |

实现要点：
- **STBN 绑定铁律**：RT shader 内禁止组合采样器声明（历史坑：破坏描述符集使材质绑定读 0），统一 `Texture3D` + `Load()` 整数坐标（PT rgen b10 / ReSTIR Init b5 / Temporal b12 / Spatial b6）。
- A-Trous 依赖时域降噪输出——`r.PT.Denoise` 关闭时自动旁路（需与降噪同时开启）。
- 新增 CVar：`r.PT.Atrous`(true) / `r.PT.Atrous.Iterations`(4) / `r.PT.Atrous.SigmaDepth`(0.05) / `r.PT.Atrous.SigmaNormal`(128) / `r.PT.Atrous.SigmaColor`(0.5) / `r.PT.Atrous.Clamp`(0)。

### 关键文件（实际）

| 文件 | 操作 | 说明 |
|------|:---:|------|
| `Engine/Shader/Shaders/PT_Common.slang` | 新建 | 共享库：PathPayload(48B)、Hash/Rand、BSDF 混合采样（余弦+GGX）、MIS、SampleLight、天空 |
| `Engine/Shader/Shaders/PathTracing/PT_Full.rgen.slang` | 新建 | 迭代式路径追踪（循环在 RayGen 内，深度 2 够用）；哨兵 shadow ray（ACCEPT_FIRST_HIT + SKIP_CLOSEST_HIT，miss 写 a=-1） |
| `Engine/Shader/Shaders/PathTracing/PT_Full.rchit.slang` | 新建 | 自带材质/法线查询副本（RT_HitCommon 绑定为 4/5/6，PTPass 布局为 5/6/7 无法共用） |
| `Engine/Shader/Shaders/PathTracing/ReSTIR_Init.comp.slang`、`ReSTIR_Temporal.comp.slang`、`ReSTIR_Spatial.comp.slang` | 新建 | WRS 蓄水池 / 时域重投影合并+写下一帧历史 / 空间邻域复用；lightIndex clamp 越界防护 |
| `Engine/Render/RT/PTPass.h/cpp` | 新建 | 4 输出 UAV RT Pass（maxPayloadSize=48，CreateEffectPipeline 复用） |
| `Engine/Render/RT/ReSTIRPass.h/cpp` | 新建 | 3 compute PSO + 蓄水池 SSBO×4 + 历史纹理双缓冲 + 槽位帧末交换 |
| `Engine/Render/Pipeline/PathTracingPipeline.h/cpp` | 新建 | 完整管线：AS_Build→PT_Render→[ReSTIR]→PT_Denoise→ToneMap→FXAA |
| `Engine/Render/Pipeline/PTQualityCVars.h/cpp` | 新建 | r.PT.* CVar 集中声明（SPP/Bounces/Denoise/ReSTIR/MIS/Roulette/降噪混合因子） |
| `Engine/Render/Pipeline/RTPass.cpp` | 修改 | BuildSceneMaterialTexture 4 行扩展 |
| `Engine/Shader/Shaders/ShaderTypes.slang` | 修改 | PTPushConstant(176B)/PTReservoir(32B)/ReSTIRPushConstant(128B) |
| `Samples/02.Cube/02.Cube.cpp` | 修改 | mode 3 分支 + PT 质量面板 + enableValidation=false（见已知问题） |

### 已知问题

1. **本机（Intel Arc B370）驱动编译器崩溃（预先存在，非本规划引入）**：
   - 开启 Vulkan 验证层时，igc-default64.dll 在 shader 编译线程随机 SIGSEGV（HEAD 版本同样复现，崩溃点随机漂移）。
   - 规避：`02.Cube.cpp` 已临时 `enableValidation=false`（附注释）。NVIDIA/其他稳定驱动上可恢复 true。
   - 附带影响：Intel 驱动 RT 管线编译极慢（~20s/个），02.Cube 启动初始化约 80-110s（多次 resize 触发重编译）。
2. ~~**ReSTIR 目标函数不含 albedo/metallic**~~ ✅ **已解决（2026-08-06）**：PT 第 5 UAV 输出 albedoMetallic，三个 `EvaluateTarget` 改用真实材质 BRDF（WRS 对 p̂ 正缩放不变，无偏性保持）。
3. **ReSTIR 估计公式**：最终着色用 RTXDI 规范 `f(y)·W·M/weightSum`（W 为当前像素重评估的目标函数值）。文档 §4.2 Pass 4 的 `W/(weightSum·M)` 公式有误（缺 M 因子，亮度会严重偏暗）——实施时已修正。
4. ~~**PT 模式不渲染粒子**~~ ✅ **已解决（2026-08-07）**：`PathTracingPipeline` 接入 `ParticleRenderer`——粒子 Billboard 复合到 PT HDR（PT_Render 之后、时域降噪之前），随降噪/ToneMap 输出；深度附件每帧清成远平面（PT 场景深度未光栅化，粒子不与场景做深度遮挡）；`02.Cube` 已注册粒子组件到 PT 管线。
5. **02.Cube 默认模式保持 2（HybridRT）**；PT 模式通过 ImGui RadioButton 或 `r.Pipeline.Mode 3` 切换；ReSTIR 默认关闭（ImGui Checkbox / `r.PT.ReSTIR 1` 开启）。

### 与文档设计的主要偏差

| 文档设计 | 实际实施 | 原因 |
|---------|---------|------|
| ReSTIR 三个 Pass 用 token 纹理链强制 RG 排序 | 三个 dispatch 放入**单个 RG Pass** 顺序执行 | CullDeadPasses 对 `writes.empty()` 的 Pass 保留；单 Pass 内提交序保证顺序，无需 token |
| 文档 §4.2 Pass 4 独立 Shade Pass | PT_RayGen 反弹 0 直接读 FinalReservoir + 1 条 shadow ray | 避免重复着色逻辑（与 Level 3 帧图一致） |
| `r.Pipeline.Mode==2` 为 PT | 2 已被 HybridRT 占用，PT 为 **3** | 引擎既有约定（0=Forward 1=Deferred 2=HybridRT 3=PathTrace） |

### 后续（阶段 C-G）

阶段 C: NRD 集成（ReBLUR/ReLAX/SIGMA，当前 PT 降噪为自研时域累积 + A-Trous）；阶段 D: ReSTIR GI/PT（GRIS）；阶段 E: SER/OMM；阶段 F: NRC（STBN 已完成）；阶段 G: 完整材质扩展。

### 2026-09-18 复核

- **定位修订**：PT 现在的定位是「参考渲染器 / Ground Truth」（`PathTracingPipeline.h` 注释），
  Level 3 实时化路线改为条件触发项；逐项取舍见新增的 **§0 现状复核（2026-09-18）**。
- 原"后续（阶段 C-G）"改写为：C（降噪）→ 接入**统一降噪框架**（`Lumen…` §5.1），NRD 为可选后端；
  D（ReSTIR GI）→ 落点 **Lumen/P6**；E/F（SER/OMM/Mega Geometry/NRC/NTC）→ 条件触发（Level 3）；
  G（材质）→ 优先 **PathPayload 扩展 + 采样端接入共享 BRDF**。
- **关键文件逐个核对（本轮）**：`PathTracingPipeline`、`RT/PTPass`、`RT/ReSTIRPass`、
  `PostProcess/RTDenoiser`、`PostProcess/PTAtrousPass`、`Pipeline/PTQualityCVars`、
  `Shaders/PT_Common.slang`、`Shaders/PathTracing/PT_Full.{rgen,rchit,rmiss}.slang`、
  `Shaders/PathTracing/ReSTIR_{Init,Temporal,Spatial}.comp.slang`、`Shaders/PathTracing/PT_Atrous.comp.slang`
  全部存在；CVar 集合与 §0.2 列出的一致。
- 本轮**未改动 PT 相关代码**，只更新本文档：定位修订 + §0 复核 + 各阶段状态标注 +
  §4.2 ReSTIR 最终着色公式就地修正 + 附录 A 依赖现状 + 本节。
