# HugEngine 功能缺口与对标分析（RHI / 渲染 / 游戏层 / 工程化）

> 合并说明：本文件由《HugEngine RHI功能缺口分析-与UE5对比》与《HugEngine规划缺口分析-对标2026游戏引擎》合并而成
> 日期信息：RHI 文档：分析 2026-09-21、复核 2026-09-22；规划文档：最后更新 2026-09-22
> 判定口径：❌ = 未实现；⚠️ = 部分实现或实现但不生效（**⚠️ 一律给出代码位置**，便于复核）
> 姊妹文档：RHI 架构细节见《HugEngine RHI架构与Vulkan实现分析.md》；架构与时序见《HugEngine架构UML与可扩展性分析.md》

---

## 目录

1. [结论先行](#1-结论先行)
2. [缺口总览（按层索引）](#2-缺口总览按层索引)
3. [核心架构差距](#3-核心架构差距)
4. [图形管线差距](#4-图形管线差距)
5. [资源管理差距](#5-资源管理差距)
6. [Compute 差距](#6-compute-差距)
7. [Ray Tracing 差距](#7-ray-tracing-差距)
8. [Work Graph / GPU Driven 差距](#8-work-graph--gpu-driven-差距)
9. [RenderGraph 实现位置与与 RDG 的差距](#9-rendergraph-实现位置与与-rdg-的差距)
10. [渲染层"未来清单"里仍然漏掉的](#10-渲染层未来清单里仍然漏掉的)
11. [跨平台与多后端](#11-跨平台与多后端)
12. [结构性空白：完全缺失的子系统](#12-结构性空白完全缺失的子系统)
13. [内容与数据管道缺口](#13-内容与数据管道缺口)
14. [工具链与工程化缺口](#14-工具链与工程化缺口)
15. [横切架构设计缺位](#15-横切架构设计缺位)
16. [有代码证据的架构债务](#16-有代码证据的架构债务)
17. [AI 规划的配套缺口](#17-ai-规划的配套缺口)
18. [规划方法论问题](#18-规划方法论问题)
19. [补入优先级（P0 / P1 / P2 / P3）](#19-补入优先级p0--p1--p2--p3)
20. [明确不需要的（UE5 RHI 中）](#20-明确不需要的ue5-rhi-中)
21. [对照检查表：缺口 → 应更新的规划文档](#21-对照检查表缺口--应更新的规划文档)
22. [参考来源与文档版本](#22-参考来源与文档版本)

---

## 第一部分 · 结论与总览

### 1. 结论先行

> **定位**（原规划文档头部）：审视《HugEngine技术全景与实施计划》(350+ 项) + 《HugEngine架构UML与可扩展性分析》 + 《HugEngine AI架构设计/》系列，对照 2025-2026 年最新商用引擎（UE5.5/5.6、Unity 6、Godot 4.x）与未来趋势，找出**规划层面**的缺失。
>
> **结论**（原规划文档头部）：HugEngine 的规划是一份非常完整的"渲染引擎"规划（甚至超前，覆盖 SIGGRAPH 2025 / SM 6.10 / RTX Kit / 3DGS），且**渲染侧的三大规划已不再是纸面项**——Nanite 虚拟几何、Lumen 式 GI、GI 分层合成架构均已落地并归档为《已实现功能》；但距离"游戏引擎"架构仍缺一层——音频、运行时 UI、输入抽象、NPC 决策/感知、通用调试渲染原语五项未立项（物理、角色移动与碰撞/射线查询、导航寻路、骨骼网格与动画重定向、通用玩法组件族已于 2026-09 落地，见 §12）。同时渲染层自身仍有已商用/将商用的空白，另有一批**有代码证据的架构债务**（Vulkan 泄漏、Scene↔RHI 耦合、序列化半残，见 §16 与《架构UML与可扩展性分析》§17）。AI 一等公民规划是亮点，其运行时层（`Engine/AI`）与物理层（`Engine/Physics`，JoltPhysics）已于 2026-09 落地，但配套的安全/测试/成本仍然缺失。

一句话总结：

> **渲染规划已经足够超前，缺的不是"更前沿的渲染"，而是"把引擎变成游戏引擎的那一层"（音频/脚本运行时/运行时 UI/输入/工具链），以及让 350+ 项特性可验证、可降级、可交付的工程化设计。**

现状画像：

| 维度 | 现状 | 判断 |
|------|------|------|
| 渲染技术覆盖 | 350+ 项，含 2026 前沿（ReSTIR PT、RTX Kit、3DGS、SM 6.10）；**且 Nanite 虚拟几何、Lumen（软件+硬件光追混合）、GI 分层合成（IBL/RSM/SSGI/SSR/DDGI/Lumen/RT 源归一化加权）已落地**，落地文档归档于 `docs/已实现功能/` | ✅ 超前，部分已兑现，其余需要裁剪 |
| 游戏层（物理/音频/脚本/UI/AI/输入） | 物理（JoltPhysics）、角色移动 + 碰撞/射线查询（`CollisionSystem`）、导航寻路（`NavMeshSystem` / `NavAgentSystem`）、骨骼网格与动画重定向（`SkeletalMeshComponent` / `SkeletalMeshSystem` / `SkeletonAsset` + `RetargetProfile`）、通用玩法组件族（约 30 个组件 + 各配套 System）均已落地并接入 ECS tick；**音频 / 运行时 UI / 输入抽象仍为零** | ⚠️ 缺口显著收窄，剩余 3 项 |
| 工具链/工程化 | doctest 单元测试套件已落地（`Tests/`：**46 个测试文件 / 328 个 TEST_CASE**，覆盖物理、AI、Lumen、Nanite、导航、反射、组件等）；崩溃处理已落地（`Engine/Core/CrashHandler`：DbgHelp 符号化 + minidump）；无 CPU Profiler、无 CI（无 `.github/`）、无金图回归 | ⚠️ 测试已具规模，但缺自动执行与回归门 |
| 横切架构（内存预算/时间/资源服务） | 仍缺位（时间系统 / AssetManager / GPU 内存预算 / 确定性） | ⚠️ 单点特性无法替代；相关"有代码证据的债务"见 §16 |
| AI 一等公民 | 设计超前（L2.5 AI 运行时层） | ✅ 亮点，配套待补 |

> RHI 文档侧：原 RHI 文档未单设"结论"章节，其结论以文首的判定口径与 §19 的优先级建议（原 RHI §9.1）呈现，另有 §20 列出的"明确不需要"项（原 RHI §9.2）。

### 2. 缺口总览（按层索引）

本表为合并后新增的层索引：把两份文档的缺口按"层"归位，优先级列对应 §19 合并后的 P0–P3，末列为详细章节。

| 层 | 主要缺口 | 优先级 | 详见 |
|----|----------|--------|------|
| RHI 核心架构 | 无专用 RHI 线程、无线程安全资源管理、无命令批处理；子资源级状态追踪、Split Barrier、跨队列自动同步缺口；无独立资源池化分配器 | P0（资源状态追踪 + 自动 Barrier）/ P1（扩展动态状态） | §3 |
| 图形管线 | 动态渲染、扩展动态状态、扩展动态图元拓扑、VRS / Fragment Shading Rate、Conservative Rasterization、Stencil / Depth Bounds、多视口 / Multi-View、Occlusion Query；两条 PSO 创建路径采样数不一致 | P0（动态渲染）/ P1（扩展动态状态）/ P2（MSAA、Subpass、Geometry、VRS） | §4、§9 |
| 资源管理 | Upload Heap、稀疏/瓦片资源、磁盘常驻纹理、Lock/Unlock 区域语义、渲染目标格式转换、自定义 MSAA resolve、纹理视图种类 | P1（Upload Heap） | §5 |
| Compute | Subgroup 操作、Cooperative Matrix / Vectors、Linear Algebra、Shader Atomic 扩展、8-bit / 16-bit 存储、Async Compute 优先级调度 | P3（Cooperative Vectors / Linear Algebra） | §6 |
| Ray Tracing | Inline Ray Tracing、SER、OMM、DMM、RT Pipeline Libraries、BuildFlags 完整映射、AABB Geometry、Multi-Hit / Multi-RayGen | P3 | §7 |
| GPU Driven / Work Graph | 统一的 GPU Scene 完整管线、GPU Work Graphs 完整能力、ExecuteIndirect 分组 | —（两份优先级清单未单列） | §8 |
| 平台（跨平台与多后端） | D3D12 / Metal / WebGPU 后端、移动端（Vulkan 后端 + ASTC/TBDR/功耗）、主机平台抽象、像素流送、多 GPU、HDR（scRGB）、VRR、全屏独占、多窗口 | P0（D3D12 后端）/ P2（移动端与主机平台抽象）/ P3（Multi-GPU / VR） | §10、§11 |
| 游戏层 | 音频、运行时 UI、输入抽象、NPC 决策与感知、通用 DebugDraw 即时原语；脚本运行时（可热更脚本层 + 统一时间系统） | P0（输入抽象、DebugDraw、时间系统）/ P1（脚本运行时、运行时 UI、AI 决策与感知） | §12 |
| 内容与数据管道 | USD、MaterialX、FBX、Cook/打包/热更、动画资产管道、AnimGraph / BlendSpace / IK / 物理动画、色彩管理全链路（OCIO） | P1（Cook/打包/热更、AnimGraph、色彩管理）/ P2（USD、MaterialX、FBX） | §13 |
| 工程化 | CPU/内存 Profiler、CI / 金图回归、Live Coding、插件/模块系统、Sequencer、崩溃遥测与 GPU fault、Shader 变体管理/缓存、PSO 预热线程 | P1（Profiler、CI/金图）/ P2（插件、Live Coding、崩溃遥测） | §14 |
| 横切架构 | GPU 内存预算管理器、运行时 AssetManager、时间系统、确定性/回放、NVIDIA 降级路径；另有代码级债务（后端抽象纪律、Scene↔RHI 耦合、序列化半残等） | P0 | §15、§16 |
| AI | 安全边界（prompt injection / 资源爆炸防护）、AI 回归测试、模型与成本管理 | P2 | §17 |
| 规划方法论 | 无验收标准、优先级失衡、无对标基线更新机制、规划碎片化 | —（方法论项，不列 P0–P3） | §18 |

---

## 第二部分 · RHI 层缺口（对标 UE5 RHI）

### 3. 核心架构差距

（原 RHI 文档 §一；其 §1.4「调试与诊断」按层归入 §14.1）

#### 3.1 多线程模型

UE5 RHI 有一套成熟的 **RHI Thread** 架构：

```
┌──────────────────────────────────────────────┐
│  Game Thread  →  Render Thread  →  RHI Thread  │  ← UE5
│  (逻辑/提交)     (可见性/剔除)    (API 调用)      │
├──────────────────────────────────────────────┤
│  Main Thread  →  JobSystem 并行录制  →  Submit   │  ← HugEngine
│  (逻辑+主 CB)     (Secondary CB)        (提交)     │
└──────────────────────────────────────────────┘
```

**缺口**：
| 特性 | UE5 | HugEngine |
|------|-----|-----------|
| 专用 RHI 线程 | `FRHIThread` 将 API 调用卸载到独立线程 | 无（Secondary CB 仅支持并行录制，提交仍在主线程） |
| 命令列表多线程录制 | `FRHICommandList` 可在任意线程创建并录制 | ⚠️ `VulkanCommandList` 支持 Secondary CB；`ForwardPipeline` 已用 JobSystem 分块并行录制（每线程一条独立 sec CL），提交仍在主线程 |
| 线程安全资源管理 | `FThreadSafeRHI` 保证跨线程安全 | 无线程安全机制 |
| RHI 命令批处理 | 命令缓冲在 Immediata/Deferred 模式间切换 | 无批处理 |

#### 3.2 资源状态追踪与自动 Barrier

UE5 的核心基础设施：

| 特性 | UE5 | HugEngine |
|------|-----|-----------|
| **自动 Barrier 生成** | `RHICmdList.Transition()` 自动插入布局转换和同步 | RHI 层只有手动 `PipelineBarrier()`；L3 的 `RenderGraph::DeriveBarriers()` 按拓扑序自动推导并插入（含导入资源真实布局查询） |
| **子资源状态追踪** | 纹理每个 subresource 独立追踪状态（mip/array/slice 级别） | 仅有整资源级别的 `ResourceState` 枚举（另有视图级 `TextureLayoutTracker` 记录真实布局） |
| **状态去重** | 引擎追踪当前状态，跳过冗余 Barrier | `DeriveBarriers()` 追踪当前状态，状态未变时不发 Barrier；RHI 层手动调用不保证去重 |
| **Split Barrier** | 支持 `VK_KHR_synchronization2` / `VK_PIPELINE_STAGE_2_*` | 仅使用传统 `VkMemoryBarrier` / `VkImageMemoryBarrier` |
| **跨队列自动同步** | `ERHIAccess` + 自动所有权转移 | RenderGraph 的 AsyncCompute 路径自动插入跨队列 Barrier 与 `QueueOwnershipTransfer()`；直接使用 RHI 时仍需手动调用 |

#### 3.3 资源池化与缓存

| 特性 | UE5 | HugEngine |
|------|-----|-----------|
| **渲染目标池** | `FRenderTargetPool` 管理瞬态纹理的分配/回收/别名 | ⚠️ 有 RenderGraph 别名分析 + `TransientResourceAllocator`（128MB × 2 Heap），但没有独立的池化分配器 |
| **PSO 缓存** | `FPipelineCacheFile` 磁盘持久化 + base/derived pipeline 派生 | ⚠️ 已有 `VkPipelineCache` 磁盘持久化 + 内存哈希复用 + GPL 四段库；未使用 base/derived pipeline 派生 |
| **Pipeline Library** | 使用 `VK_KHR_pipeline_library` 加速 PSO 编译 | ⚠️ 已用于 GPL 四段库 + fast-link（图形管线；RT 管线未使用） |
| **Pipeline Derivatives** | 利用 `VK_PIPELINE_CREATE_DERIVATIVE_BIT` 快速创建变体 | 未使用 |
| **描述符集缓存** | 线程本地描述符堆，按需分配和复用 | 全局单一 `VkDescriptorPool` + 设备级 bindless 堆（槽位环形分配/回收） |
| **上传堆** | `FRHIUploadHeap` 环形缓冲池化动态数据上传 | `BufferDesc::initialData` 每次创建 staging buffer |
| **瞬态资源管理** | `RHICmdList::CreateTransientResource` 内存别名、lazy allocation | ⚠️ 有 `TransientResourceAllocator`（帧内 bump 子分配 + VkImage 缓存 + 双缓冲 Heap），RenderGraph 对所有别名资源走此路径 |
| **纹理流送** | `FRHITexture` 支持部分常驻（sparse binding / tiled resources） | 无稀疏纹理支持 |

### 4. 图形管线差距

#### 4.1 管线阶段支持

| 着色器阶段 | UE5 | HugEngine |
|------------|-----|-----------|
| **Vertex** | ✅ | ✅ |
| **Pixel/Fragment** | ✅ | ✅ |
| **Compute** | ✅ | ✅ |
| **Geometry** | ✅ | ❌ Types.h 中定义了 `ShaderStage::Geometry`，但 Pipeline 创建未实现 |
| **Tessellation (Hull/Domain)** | ✅ (Nanite 上线后较少使用) | ❌ 未定义 Hull/Domain ShaderStage |
| **Mesh** | ✅ | ✅ |
| **Amplification/Task** | ✅ | ✅ |
| **RayGen/Miss/Hit/Callable** | ✅ | ✅ |

#### 4.2 管线状态覆盖

| 状态能力 | UE5 | HugEngine |
|----------|-----|-----------|
| **动态渲染** (`VK_KHR_dynamic_rendering`) | ✅ UE5.2+ (减少 VkRenderPass 对象，按需构建渲染通道) | ❌ 仍使用传统 VkRenderPass + VkFramebuffer |
| **扩展动态状态** (`VK_EXT_extended_dynamic_state`) | ✅ (减少 PSO 变体) | ❌ 仅有 viewport + scissor 两个动态状态 |
| **动态图元拓扑** (`VK_EXT_extended_dynamic_state2`) | ✅ | ❌ 固定在 PSO 创建时 |
| **动态多边形模式** | ✅ (线框/填充切换无需变 PSO) | ❌ PSO 创建时可配置 `FillMode`（Solid/Wireframe），非动态状态 |
| **动态 Cull Mode / Front Face** | ✅ | ❌ PSO 创建时可配置 `cullMode` / `frontFace`（默认 `CullMode::None` / `FrontFace::Clockwise`），非动态状态 |
| **动态深度测试状态** | ✅ (depthTestEnable / depthWriteEnable / depthCompareOp 动态) | ❌ 固定在 PSO |
| **动态混合状态** | ✅ | ❌ 固定在 PSO |
| **动态顶点输入** (`VK_EXT_vertex_input_dynamic_state`) | ✅ | ❌ 绑定在 PSO |
| **动态 MSAA 状态** | ✅ | ❌ |

> **MSAA 的三层口径分裂（2026-09-22 逐条核实的结论：MSAA 实际不生效）**
>
> **① 上层从不触发覆盖。** `AA_MSAA` 的全部实现就是覆写 `d.sampleCount`（`AA_MSAA.h:58/62`），但 `OverrideTextureDesc` / `OverridePSODesc` 在 `Engine/` 与 `Samples/` 中**只有声明（`AntiAliasing.h:72/75`）与实现，零调用点**；`DeferredPipeline.cpp:68-71` 里与 MSAA 对应的**只有一行 `HE_CORE_INFO` 日志**——其上方注释"覆盖纹理和 PSO 的 sampleCount（委托给 PostProcessChain）"是**失效注释**。`PostProcessChain::EnableMSAA` 只置标志位（`PostProcessChain.cpp:148-155`），`AA_MSAA::OnResize` 也从未被 `PostProcessChain::OnResize` 调用（`PostProcessChain.cpp:90-122`）。
>
> **② RHI 三条路径对采样数的处理互不一致。**
>
> | 路径 | 采样数处理 | 位置 |
> |---|---|---|
> | 运行时图形 PSO | **硬编码** `VK_SAMPLE_COUNT_1_BIT`（忽略字段） | `VulkanPipeline.cpp:307` |
> | 运行时 Mesh Shader PSO | **硬编码** `VK_SAMPLE_COUNT_1_BIT` | `VulkanPipeline.cpp:596` |
> | 后台预热 PSO | **读** `desc.sampleCount`，且是**裸 `static_cast`**（0 会被转成 0，而非退化到 1） | `PSOPrecompileManager.cpp:376` |
> | 纹理创建 | **正确**用 `switch` 映射 `{2,4,8,16,32,64}`，default 1 | `VulkanResources.cpp:333-350`、`VulkanDevice.cpp:1287-1304` |
>
> **③ 缓存 key 两侧都计入 `sampleCount`**（`VulkanPipeline.cpp:110`、`VulkanPipelineLibrary.cpp:86`），因此预热按 key 命中的"多采样管线"与运行时实际使用的 1x 管线**不是同一个东西，且查不到错**。
>
> **结论与修法**：MSAA 目前是"代码齐全但从未生效"（详见《HugEngine后处理与抗锯齿实现分析》§6）。修法三步：① 决定 MSAA 归谁管——要么让上层真正调用覆盖钩子（并在两处 PSO 路径统一读 `desc.sampleCount`），要么删掉覆盖钩子并把预热路径也固定为 1；② 顺手去掉 `DeferredPipeline.cpp:68` 的失效注释；③ 若保留硬件 MSAA，需同时补自定义 resolve（当前无）。

#### 4.3 渲染特性

| 特性 | UE5 | HugEngine |
|------|-----|-----------|
| **Subpass Input Attachment** | ✅ (移动端 TBDR 优化) | ❌ `DescriptorType::InputAttachment` 已定义但未实现绑定 |
| **MSAA** | ✅ (硬件 MSAA 2/4/8x + 自定义 resolve) | ❌ **实际不生效**（`AA_MSAA` 的覆盖钩子零调用点、RHI 运行时 PSO 硬编码采样数 1、且无自定义 resolve）——代码存在但从未被触发，`AntiAliasing.h:88-91` 的"MSAA=true（硬件采样覆盖 RT）"是设计意图而非当前行为；完整证据见《HugEngine后处理与抗锯齿实现分析》§6 与 §4.2 注 |
| **Conservative Rasterization** | ✅ | ❌ |
| **Variable Rate Shading (VRS)** | ✅ | ❌ (DeviceCaps 有 `supportsVRS` 标志，无实现) |
| **Fragment Shading Rate** | ✅ | ❌ |
| **Depth Bounds Test** | ✅ | ❌ `depthBoundsTestEnable = VK_FALSE` |
| **Stencil Operation** | ✅ (完整的 stencil 状态) | ❌ `stencilTestEnable = VK_FALSE`，无 stencil 接口 |
| **Alpha-to-Coverage** | ✅ | ❌ |
| **Sample Rate Shading** | ✅ | ❌ |
| **多视口渲染** | ✅ (VR / 瀑布 / cubemap 单通道) | ❌ viewportCount 固定为 1 |
| **VR 支持 (Multi-View)** | ✅ (VK_KHR_multiview) | ❌ |
| **Occlusion Query** | ✅ (硬件遮挡查询) | ❌ 无遮挡查询（查询池支持 Timestamp / PipelineStatistics 两类） |
| **Pipeline Statistics** | ✅ | ⚠️ 已支持 `VK_QUERY_TYPE_PIPELINE_STATISTICS`（6 个计数器） |
| **Stream Output (Transform Feedback)** | ✅ (旧版，逐步淘汰) | ❌ |

#### 4.4 离屏渲染

| 特性 | UE5 | HugEngine |
|------|-----|-----------|
| **Render Dependency Graph** | ✅ 完整的 RDG（资源生命周期管理、状态推导、Pass 合并、图可视化） | ⚠️ 已在 **Render 层**实现基础 RenderGraph（`Engine/Render/RenderGraph.{h,cpp}`，约 756 行）：依赖图构建（RAW/WAW/WAR）+ 拓扑排序 + 自动 Barrier 推导与状态去重 + 死 Pass 裁剪 + 资源别名 + AsyncCompute 调度 + Profiler 集成，`DeferredPipeline::BuildFrameGraph()` 与 `ForwardPipeline` 均已使用；**仍缺**：图可视化/导出、Pass 合并优化、显式外部依赖 API（`AddExternalAccess`/`AddReadback`）、子资源级追踪、Scoped 资源生命周期、Pass 条件执行（完整清单见 §9） |
| **Graph 导出/可视化** | ✅ | ❌ |
| **Pass 剔除** | ✅ (无输出的 Pass 自动跳过) | ✅ `CullDeadPasses()`：输出未被消费的 Pass 从执行列表移除 |
| **显式依赖表达** | ✅ (`AddReadback()`, `AddExternalAccess()`) | ❌ |

### 5. 资源管理差距

#### 5.1 缓冲

| 特性 | UE5 | HugEngine |
|------|-----|-----------|
| **结构化缓冲** | ✅ (RWStructuredBuffer, ByteAddressBuffer) | ⚠️ 基础 Storage Buffer，无结构化语义 |
| **Upload Heap** | ✅ (环形缓冲 + 围栏同步) | ❌ 每次 `initialData` 创建临时 staging buffer |
| **Readback Heap** | ✅ (GPU→CPU 回读管线) | ⚠️ `Map()`/`Unmap()` 同步回读 + `CopyTextureToBuffer`（GPU 端纹理→缓冲拷贝），无专用回读堆 |
| **稀疏/瓦片资源** | ✅ (VK_EXT_sparse_residency) | ❌ |
| **资源别名** | ✅ (同一内存复用) | ⚠️ RenderGraph `ApplyAliasing()` 做生命周期分析并让别名资源走瞬态内存池，非同一 offset 的显式内存别名 |
| **磁盘常驻纹理** | ✅ (部分 mip 常驻) | ❌ |
| **Lock/Unlock 语义** | ✅ (区域锁定，非整缓冲) | ❌ `Map()` 返回整缓冲指针 |

#### 5.2 纹理

| 特性 | UE5 | HugEngine |
|------|-----|-----------|
| **纹理数组 / Atlas** | ✅ 通用支持 | ⚠️ 基础 `arrayLayers` 支持 |
| **体积纹理 (3D)** | ✅ | ⚠️ `depth > 1` 时创建 VK_IMAGE_TYPE_3D |
| **MSAA 纹理** | ✅ 含 resolve | ⚠️ 纹理侧 `sampleCount` 是**可用**的（`VulkanResources.cpp:333-350` 用 `switch` 正确映射，`AA_MSAA.h:60/64` 会覆写该字段），但**没有任何调用点触发覆盖**、也**无自定义 resolve 路径** ⇒ 当前不产生多采样光栅化（见 §4.2 注与 §4.3） |
| **纹理视图 (SRV/UAV/RTV/DSV)** | ✅ 多种视图（不同格式/子资源） | ⚠️ 仅有 Per-Mip 存储/采样视图（含 mip+layer 变体） |
| **Clear/Copy 专用路径** | ✅ 快速清除和拷贝（无需渲染通道） | ⚠️ 有 `ClearDepthStencil` / `CopyTextureToTexture` / `CopyTextureToBuffer`，仍无 `ClearRenderTarget` 接口 |
| **Mipmap 生成 API** | ✅ `GenerateMipMaps()` | ⚠️ 仅在 `initialData` 上传时自动生成 |
| **渲染目标格式转换** | ✅ (R11G11B10 → HDR 等) | ❌ |

### 6. Compute 差距

| 特性 | UE5 | HugEngine |
|------|-----|-----------|
| **Async Compute 调度策略** | ✅ 优先级调度、依赖图自动识别可异步 Pass | ⚠️ 队列层有 Tier 0-2 检测；RenderGraph 有 `ScheduleAsyncPasses()`（标记无依赖 Compute Pass）+ `ExecuteWithAsyncCompute()`（自动拆分 Graphics/Compute 并跨队列同步），但无优先级调度 |
| **Subgroup 操作** | ✅ (wave/warp 内通信) | ❌ |
| **Cooperative Matrix** | ✅ (VK_KHR_cooperative_matrix / 硬件矩阵乘法) | ❌ (DeviceCaps 无此标志) |
| **Cooperative Vectors** | ✅ (VK_KHR_cooperative_vector) | ❌ (DeviceCaps 有 `supportsCooperativeVectors` 标志，无实现) |
| **Linear Algebra** | ✅ (VK_KHR_linear_algebra / 硬件矩阵/向量加速) | ❌ (DeviceCaps 有 `supportsLinearAlgebra` 标志，无实现) |
| **Shader Atomic 扩展** | ✅ (VK_EXT_shader_atomic_float / int64 atomics) | ❌ |
| **8-bit / 16-bit 存储** | ✅ (VK_KHR_16bit_storage / VK_KHR_8bit_storage) | ❌ |

### 7. Ray Tracing 差距

| 特性 | UE5 | HugEngine |
|------|-----|-----------|
| **Inline Ray Tracing** | ✅ (VK_KHR_ray_tracing_maintenance1 / 无需单独 RT PSO) | ❌ |
| **Shader Execution Reordering (SER)** | ✅ (硬件 reorder 提升 coherence) | ❌ (DeviceCaps 有 `supportsSER` 标志，无实现) |
| **Opacity Micromaps (OMM)** | ✅ (硬件透明遮罩) | ❌ (DeviceCaps 有 `supportsOMM` 标志，无实现) |
| **Displacement Micromaps (DMM)** | ✅ (UE5.4+) | ❌ |
| **RT Pipeline Libraries** | ✅ (`VK_KHR_pipeline_library`) | ❌ 每次重建完整 RT PSO |
| **BuildFlags 完整映射** | ✅ `AllowCompaction` 含自动压缩步骤 | ⚠️ `AllowCompaction` 仅在 `ToVkBuildFlags` 中映射，无后续压缩逻辑 |
| **AABB Geometry** | ✅ (程序化几何) | ⚠️ `RTGeometryType::AABBs` 已定义但未在 BuildBLAS 中处理 |
| **Multi-Hit / Multi-RayGen** | ✅ | ❌ `RTPipelineStateDesc` 无相关配置 |

### 8. Work Graph / GPU Driven 差距

| 特性 | UE5 | HugEngine |
|------|-----|-----------|
| **GPU Work Graphs** | ✅ (VK_EXT_device_generated_commands 用于复杂 GPU 端工作) | ⚠️ 基础 DGC 支持（EXECUTION_SET + DRAW_INDEXED 两个令牌） |
| **GPU Scene 完整管线** | ✅ (GPU 端剔除 → LOD 选择 → 间接绘制) | ⚠️ 已有 GPU 侧视锥/Hi-Z 遮挡剔除（`GPUCulling` / `InstanceCuller`）与 Nanite 簇层次剔除 + LOD 选择 + 间接绘制（含 DGC 路径），但不是单一统一的 GPU Scene 管线 |
| **Multi-Draw Indirect (MDI)** | ✅ 含 CountBuffer | ✅ `DrawIndexedIndirect` / `DrawIndexedIndirectCount` / `DrawMeshTasksIndirectCount`（GPU 侧计数缓冲） |
| **ExecuteIndirect 分组** | ✅ (按材质/Pipeline 分组) | ❌ |

### 9. RenderGraph 实现位置与与 RDG 的差距

> 本章原为"更正"章节——原 RHI 文档初版曾把 RenderGraph 误判为未实现。**正文口径已修正**：§4.4 现在直接给出 RenderGraph 的实现范围与缺口，本章只保留实现位置、功能入口与与 UE5 RDG 的逐项差距，避免同一内容两处维护。

#### 9.1 实现位置

`Engine/Render/RenderGraph.h` + `RenderGraph.cpp`（约 756 行），属于 L3 Render 层，构建在 RHI 之上。

#### 9.2 功能入口与细节（摘要见正文 §4.4，此处为明细）

| 功能 | 状态 | 实现细节 |
|------|------|----------|
| **自动 Barrier 推导** | ✅ `DeriveBarriers()` | 按拓扑序遍历 Pass，追踪每个资源的 R→W 状态变换，自动插入 PipelineBarrier |
| **依赖图构建** | ✅ `BuildDependencies()` | 完整 RAW/WAW/WAR 三种数据依赖分析 |
| **拓扑排序** | ✅ `TopologicalSort()` | 基于入度的 Kahn 算法，确定 Pass 执行顺序 |
| **死 Pass 裁剪** | ✅ `CullDeadPasses()` | 输出未被消费的 Pass 自动从执行列表中移除 |
| **资源别名** | ✅ `ApplyAliasing()` | 贪心算法分析资源生命周期，非重叠区间共享内存池 |
| **AsyncCompute 调度** | ✅ `ScheduleAsyncPasses()` | 检测无依赖的 Compute Pass 标记为可异步执行，自动插入跨队列 Barrier |
| **AsyncCompute 多阶段提交** | ✅ `ExecuteWithAsyncCompute()` | 自动拆分 Graphics/Compute 到独立 CommandList，通过 Timeline Semaphore 同步 |
| **Import 外部资源** | ✅ `ImportTexture()` | 支持导入已存在的纹理（BackBuffer、GBuffer 等） |
| **Profiler 集成** | ✅ | 每个 Pass 自动插入 `BeginPass/EndPass` + GPU 时间戳 |
| **生产管线集成** | ✅ | `DeferredPipeline::BuildFrameGraph()` 和 `ForwardPipeline` 均已使用 |

#### 9.3 宏辅助

```cpp
#define RG_READ(h)   PassResource{h, ResourceAccess::Read}
#define RG_WRITE(h)  PassResource{h, ResourceAccess::Write}
#define RG_RW(h)     PassResource{h, ResourceAccess::ReadWrite}
```

#### 9.4 与 UE5 RDG 的差距

| UE5 特性 | HugEngine 现状 |
|----------|---------------|
| **图可视化** (RDG Dump/Insights) | ❌ 无导出功能 |
| **Pass 合并** (相邻 Pass 合为一个 RenderPass) | ❌ 每 Pass 独立执行 |
| **显式外部依赖 API** (`AddExternalAccess`, `AddReadback`) | ❌ 仅有 `ImportTexture` |
| **Scoped 资源创建** (`GraphBuilder.CreateTexture` 自动生命周期) | ⚠️ RG 在 `Execute()` 时按 `ResourceDesc` 统一创建资源，但无 RAII/Scoped 生命周期 API |
| **Pass 条件执行** (`if (IsEnabled)`) | ❌ 无 |
| **异步 Compute 自动调度** | ⚠️ 需通过 `RGPassQueue::Compute` 显式标记 |
| **瞬态资源池** (RDG Transient Resource Allocator) | ⚠️ 别名资源在 `Execute()` 中走 `CreateTransientTexture`（TransientResourceAllocator 的 Heap + VkImage 缓存），未做显式的 offset 级内存别名 |
| **子资源级追踪** (mip/array slice 级别 Barrier) | ❌ 仅整资源级别 |
| **Resource->Pass 反向索引** (谁生产了此资源) | ❌ 需遍历查找 |

---

## 第三部分 · 渲染层与平台缺口

### 10. 渲染层"未来清单"里仍然漏掉的

技术全景已追到 2026 前沿，但对照 UE5.5/5.6 与 2026 趋势仍有空白：

| # | 特性 | 说明 | 优先级建议 |
|---|------|------|-----------|
| 1 | **Virtual Heightfield Mesh（VHM）** | UE5.5+ Nanite 地形：虚拟高度场地形与 Nanite 合一；你们有 Nanite 和 Terrain，但没有合并方案 | 🟡 重要 |
| 2 | **水系统** | UE5 Water：Gerstner 波、浮力、水深着色、河流网络 | 🟢 进阶 |
| 3 | **DirectStorage / GPU 解压** | 纹理/几何流式加载时缺 GDeflate/BCpack GPU 解压；IO 带宽是大世界 + 虚拟纹理（SVT/VSM）的关键瓶颈——现有 SVT/VSM 规划没有配套 IO 层设计 | 🟡 重要 |
| 4 | **移动端硬件 RT** | Adreno/Mali 的 Vulkan RT 未提 | 🟢 进阶 |

> 原规划文档 §4 共 7 项；其中「移动端低功耗路径」「主机平台抽象」「像素流送 / 云渲染」属平台侧缺口，已并入 §11 跨平台与多后端，本表只保留渲染特性类漏项。

### 11. 跨平台与多后端

> 本章为合并去重结果：主体为原 RHI 文档 §七（10 行），并并入原规划文档 §4 的「移动端低功耗路径」「主机平台抽象」「像素流送 / 云渲染」三项。其中「移动 Vulkan」与「移动端低功耗路径」是同一主题的两个侧面（**后端支持** vs **功耗优化**），已合成一行，两侧独有信息均保留。

| 特性 | UE5 | HugEngine 现状 | 优先级建议 |
|------|-----|----------------|-----------|
| **D3D12 后端** | ✅ | ❌ (仅 `Backend::D3D12` 枚举占位，无实现文件) | — |
| **Metal 后端** | ✅ | ❌ (仅 `Backend::Metal` 枚举占位) | — |
| **WebGPU 后端** | ✅ (实验性) | ❌ (仅 `Backend::WebGPU` 枚举占位) | — |
| **移动 Vulkan / 移动端低功耗路径** | ✅ (Android + Vulkan 1.1+) | ❌ (仅 Win32，使用 Vulkan 1.3+ 特性)；移动专项缺失：Metal 后端暗示 iOS，但无移动专项：ASTC、TBDR 利用、功耗管理（Subpass Input Attachment 未实现见 §4.3，但无移动端规划条目） | 🟡 重要 |
| **多 GPU 支持** | ✅ (CrossFire/SLI/NVLink/mGPU VR SLI) | ❌ | — |
| **HDR 输出** | ✅ (HDR10 / scRGB / HDR 显示器) | ⚠️ 有 HDR10 交换链（`SwapChainDesc::hdr` → A2B10G10R10 + ST.2084），无 scRGB 等其他色彩空间 | — |
| **VRR (FreeSync/GSync)** | ✅ | ❌ (仅 Mailbox/FIFO) | — |
| **全屏独占模式** | ✅ | ❌ (无全屏模式管理) | — |
| **多窗口/多 Viewport** | ✅ | ❌ | — |
| **Wayland 支持** | ❌ (UE5 主要 Win32) | ❌ | — |
| **主机平台抽象** | — | 只提了 PSSR；PS5/Xbox 的内存对齐、SDK 抽象零规划 | 🟢 进阶 |
| **像素流送 / 云渲染** | — | WebGPU 有了，但 UE Pixel Streaming 式远程渲染没有 | 🟢 进阶 |

---

## 第四部分 · 游戏层与内容管道缺口

### 12. 结构性空白：完全缺失的子系统

> 以下子系统中，**物理、角色移动、碰撞/射线查询、导航寻路、通用玩法组件族已落地**（见各行标注）；其余在 350+ 项特性清单里**仍无规划条目**（或在架构图中只有一行字），属于结构性空白，应作为新特性族补入。

| # | 子系统 | 现状（对照现有文档） | 现代引擎对标（UE5 / Unity / Godot） | 缺失影响 |
|---|--------|----------------------|-------------------------------------|----------|
| 1 | **物理系统** ✅ *已落地（2026-09）* | `Engine/Physics/` 已集成 **JoltPhysics**：`PhysicsWorld`（封装 `JPH::PhysicsSystem`，碰撞层 `NON_MOVING`/`MOVING`，maxBodies=1024）、`PhysicsSystem::Update` 固定步长 **1/120s**、`RigidBodyComponent`（Sphere/Box/Capsule 纯数据组件，Scene 层不依赖 Jolt）；`CollisionSystem` 提供 Overlap / Contains / **Raycast**（含命中法线，供地面检测与弹道用），`CharacterMovementComponent` + `MovementSystem` 提供角色移动（走/跳/重力/地面检测）；但《技术全景》特性清单中**仍为零项**（未回写特性表） | Chaos（UE5）/ PhysX（Unity）/ Jolt（Godot）：刚体、碰撞、角色控制器、射线/形状查询 API | 刚体 + 碰撞 + 射线查询 + 角色移动 + 固定步长 tick 耦合已解决；**ragdoll / 物理动画、坡度过滤与水平碰撞滑移（MVP 约定可穿墙）、raycast 的多形状查询仍缺** |
| 2 | **音频系统** | 架构 L6 画了 "Audio System" 方块，特性清单零项 | 空间音频、流式回放、混音总线、HRTF（Wwise/FMOD 或自研） | 多线程音频架构需早期预留；否则后期硬塞 |
| 3 | **脚本运行时（游戏逻辑运行时）** | ⚠️ *部分落地*：引擎侧玩法组件族已成型并**全部收口**（`Engine/Scene/Scene/` 约 30 个组件 + 各配套 System：`CharacterMovementComponent`/`MovementSystem`、`HealthComponent`/`DamageSystem`、`AbilityComponent`/`AbilitySystem`（简化 GAS：冷却计时）、`ProjectileMovementComponent`/`ProjectileSystem`、`SpringArmComponent`/`SpringArmSystem`、`SplineComponent`/`SplineMeshComponent`/`SplineSystem`/`SplineMeshSystem`、`NavMeshComponent`/`NavMeshSystem`、`NavAgentComponent`/`NavAgentSystem`、`SkeletalMeshComponent`/`SkeletalMeshSystem`、`ParticleComponent`、`DecalComponent`、`CollisionDebugComponent`/`CollisionDebugSystem`、`TextRenderComponent`/`TextRenderSystem`、`LevelComponent` 等），均有单测且在 `02.Cube`、`07.AISamples` 中被驱动；《Entity Component 开发计划》任务 1~26 已全部完成并归档；但《技术全景》只有 24.43 Visual Scripting（🟢 P5）、25.37 脚本绑定（🟢） | GameMode/PlayerController/Pawn 生命周期、固定时间步长、Tick 分级（Actor/Component/渲染）、时间膨胀 | **脚本运行时仍是分水岭**：玩法组件已就位，但缺可热更的脚本层与统一时间系统（时间膨胀/固定步长调度） |
| 4 | **运行时 UI** | 只有 ImGui 编辑器 UI | UMG 式：Canvas / 布局 / 事件 / 数据绑定 | ImGui 是工具 UI，不能当游戏 UI；缺游戏内 HUD/菜单体系 |
| 5 | **游戏 AI（NPC 行为）** | ⚠️ *寻路已落地*：`NavMeshComponent` + `NavMeshSystem`（网格 8 向 A*，对角不穿角）+ `NavAgentSystem`（每帧沿路径点移动、到达/不可达判定）；`Engine/AI/Agent` 的 LLM 智能体属**内容/编辑器侧**。**仍缺**：行为树 / 状态机 AI、感知系统、EQS | Recast/Detour 寻路、Behavior Tree、EQS（UE5） | 注意区分：`HugEngine AI架构设计/` 文档规划的是 **LLM Agent（内容生成/编辑器智能体）**，不是 NPC 行为 AI——两套东西；NPC 侧现在的空缺是**决策与感知**，不是寻路 |
| 6 | **输入抽象** | 只有 CameraController + GLFW 原始事件 | Input Action / 键位重绑 / 手柄 / 触屏统一 | 多平台（Web/移动/主机）输入必须抽象层，进 Phase 1 |
| 7 | **DebugDraw（调试渲染原语）** | ⚠️ *部分落地*：`CollisionDebugSystem` 生成碰撞体线框（AABB / Sphere / Capsule，与检测共用同一份世界形状语义，走既有网格渲染路径）、`TextRenderComponent` / `TextRenderSystem` 提供世界空间文字；**仍缺**：通用即时原语（DrawDebugLine / Box / Sphere / Point，任意颜色与存活时长，无需挂组件） | DrawDebugLine/Box/Sphere/Text（UE） | 现有实现必须"挂组件 + 走网格路径"，临时调试仍需通用即时原语；应进 Phase 1 核心 |

### 13. 内容与数据管道缺口

| # | 缺口 | 说明 | 优先级建议 |
|---|------|------|-----------|
| 1 | **USD 支持** | 未来互操作标准（Omniverse / UE5.4+ 主力）；否则引擎永远是 "glTF 播放器" | 🟡 重要 |
| 2 | **MaterialX** | 材质互操作标准，USD 生产管线标配；与 Slang 的桥接值得规划 | 🟡 重要 |
| 3 | **FBX / 商业 DCC 交换** | 动画重定向、美术生产流程需要；glTF 覆盖不全 | 🟢 进阶 |
| 4 | **Cook / 打包 / 热更 / 补丁** | 23.3 "Asset Pipeline" 一笔带过；无 cook 管线、chunk 分块、patch、DLC、版本化设计 | 🔴 核心（决定能否"发布游戏"） |
| 5 | **动画资产管道** | 动画文件格式、压缩、骨骼重定向 | 🟡 重要 |
| 6 | **动画状态机（AnimGraph）/ BlendSpace / IK / 物理动画** | ⚠️ *部分落地*：骨骼网格播放已落地——`SkeletalMeshComponent` / `SkeletalMeshSystem` / `SkeletonAsset`（glTF 骨骼加载见 `TestSkeletonLoader`），**剪辑混合**与**动画重定向**（`RetargetProfile` + `BuildRetargetProfile`，任务 21/22）已完成；**仍缺**：状态机（AnimGraph）、混合空间（BlendSpace）、IK（CCD/FABRIK）、ragdoll / 物理动画——UE5 核心动画栈在真实引擎里是 30+ 项 | 🔴 核心 |
| 7 | **色彩管理全链路（OCIO）** | 仅 3DGS 提了一句 OCIO；sRGB/BT.2020/P3 工作流、HDR 元数据、ACES 管线应是一份横切设计文档而非散点 | 🟡 重要 |

---

## 第五部分 · 工程化与横切架构

### 14. 工具链与工程化缺口

> 本章合并三份来源：原 RHI 文档 §1.4（调试与诊断）与 §八（工具链与资产管线）合并为 **14.1 GPU 侧诊断**；原规划文档 §5（工具链与工程化缺口）为 **14.2 CPU / 工程侧**。两处重叠项——**Profiler** 与**崩溃处理**——已各合成一行并保留两侧独有信息（其条目化处理见 §19 的 P1 / P2 行）。

#### 14.1 GPU 侧诊断（原 RHI 文档 §1.4 + §八）

| 特性 | UE5 | HugEngine |
|------|-----|-----------|
| **事件标记** | 每个 Draw/Dispatch 自动插入 `PROFILE_Gpu(RenderPass)` | ⚠️ 有 `VK_EXT_debug_utils` 标签（Pass 边界 Begin/End + DrawCall 级 `SetDrawDebugLabel`，受 `r.Debug.DrawMarker` 控制），非逐 Draw 自动插入 |
| **资源命名** | `BindDebugLabelName()` 对所有资源命名 | ⚠️ `SetResourceDebugName` 覆盖 Buffer/Texture/PSO/Sampler/AS 五类资源 |
| **管线统计** | `VK_QUERY_TYPE_PIPELINE_STATISTICS` 查询 VS/PS 调用次数 | ⚠️ 已支持 `VK_QUERY_TYPE_PIPELINE_STATISTICS`（IA/VS/裁剪/FS 共 6 个计数器） |
| **着色器调试** | `VK_KHR_shader_non_semantic_info` + shader printf | 无 |
| **GPU 验证** | `VK_EXT_gpu_validation` / GPU-Based Validation | 仅 `VK_LAYER_KHRONOS_validation` |
| **Shader 编译管线** | ✅ `DXC` + shader reflection + binding 自动发现 | ⚠️ 预编译 SPIR-V，binding 手动指定 |
| **PSO 预热/预编译** | ✅ (PSO precaching 系统) | ⚠️ 有 `PSOPrecompileManager`（独立 VkPipelineCache + 后台线程 + 合并回主缓存），注册与逐帧限流队列已接入生产管线，但后台预热线程的启动当前在 `DeferredPipeline` 中被注释掉 |
| **Shader 变体管理** | ✅ (自动排列组合 + permutation reduction) | ❌ |
| **Shader 缓存** | ✅ (磁盘 + 内存 LRU) | ❌ |
| **RenderDoc 深度集成** | ✅ (renderdoc_app.h) | ❌ |

> 本表前 5 行来自原 RHI §1.4，后 5 行来自原 RHI §八（两张表列结构相同，合并为一表）。原 RHI §1.4 的「**GPU 崩溃定位**」与「**GPU Profiler**」两行分别与本表下方的 §14.2 第 6 行、第 1 行为同一主题，已合成一行并移入 §14.2（保留两侧独有信息）。

#### 14.2 CPU / 工程侧（原规划文档 §5）

| # | 缺口 | 现状 | 建议方案 |
|---|------|------|----------|
| 1 | **CPU / 内存 Profiler**（并入原 RHI §1.4「GPU Profiler」） | 只有 GPU 时间戳 + ImGui 面板；RHI 侧现状：⚠️ 有 `ProfilerManager`（逐 Pass/Scope 时间戳 + 调试标签 + Profiler 面板），无 Tracy/RenderDoc 集成（UE5 对标：`Tracy`/`RenderDoc` 集成 + 内置 `Stat GPU` 系统） | 直接规划 **Tracy** 集成（CPU 采样 + 内存追踪），现代引擎标配；另补内存碎片分析、帧分析器 |
| 2 | **CI / 金图测试落地** | doctest 单元测试套件已落地（`Tests/`）；架构文档写了测试金字塔（截图对比 PSNR>40dB 等），但 WBS 里 CI 仅 ST-01.02 一行，无 CI 执行 | GitHub Actions + 金图对比工具选型 + 夜间回归：350+ 项特性没有自动验证的 CI 就是裸奔 |
| 3 | **Live Coding** | 只有 Shader 热重载 | C++ 游戏代码热重载（UE Live Coding 模式） |
| 4 | **插件 / 模块系统** | 无第三方插件 API、无模块动态加载 | UE Plugin / Unity Package 式扩展点，直接影响产品化程度 |
| 5 | **Sequencer / 过场系统** | 编辑器 53 项无时间线/过场编辑器 | 过场动画是游戏引擎标准功能 |
| 6 | **崩溃处理 / 遥测**（并入原 RHI §1.4「GPU 崩溃定位」） | ⚠️ *崩溃处理已落地*：`Engine/Core/CrashHandler`（DbgHelp 符号化调用栈 + minidump，引擎与测试共用；记录后返回 `EXCEPTION_CONTINUE_SEARCH` 保留 WER）；**仍缺**：D3D12 `DEVICE_REMOVED` / GPU fault 诊断、遥测采集；RHI 侧现状：GPU 崩溃定位对标 UE5 `BreadcrumbContext` 记录 GPU 进度、崩溃时定位问题命令 → HugEngine 无 | 补 GPU fault / 设备丢失处理 + 崩溃报告聚合 + 遥测采集 |

### 15. 横切架构设计缺位

> 这些是"单点特性"解决不了、必须单独写设计文档的架构决策。

| # | 设计项 | 说明 |
|---|--------|------|
| 1 | **GPU 内存预算管理器** | VSM / VT / Nanite / 3DGS / RT 五个吃 VRAM 大户的共存预算：谁可挤谁、优先级、降级策略——目前各自为政 |
| 2 | **运行时异步资源加载服务（AssetManager）** | Asset Registry 是编辑器向；缺运行时 AssetManager：依赖图 / 引用计数 / 加载优先级 / GC，作为核心服务 |
| 3 | **时间系统** | 固定步长模拟、时间膨胀、慢动作、回放时间线；目前只有 deltaTime 直传 |
| 4 | **确定性 / 回放** | 网络复制（2D.4 🟢）只有一句；无确定性设计、无回放系统——决定未来能否做网络游戏 |
| 5 | **NVIDIA 降级路径** | RTX Kit / DLSS / NRC / RTXNS / RTXTS 一大片 🟡 全是 NVIDIA 专属；风险表只写了 Streamline 隔离，RTXNS/NRC/RTXTS 在无 NVIDIA 硬件时的降级（CPU 回退 / 算法替代）未设计 |

### 16. 有代码证据的架构债务

> 与上表的"未立项"不同，以下几条**代码已经存在**，但结构上会让后续扩展变贵；完整证据链（文件:行号）见《HugEngine架构UML与可扩展性分析》§17，此处只记规划含义。

| # | 债务 | 规划含义 |
|---|------|----------|
| 1 | **后端抽象纪律失守**：`GI_RSM.cpp` 直接 include `Vulkan/VulkanResources.h`（死依赖）、`ImGuiIntegration.cpp` 经 `VulkanDeviceAccess` 取 `VkInstance/VkDevice`；RHI 接口自身有 `void*` 逃逸口（`GetNativeHandle` / `BeginOffscreenPass(void*)` / `GetBackendFormat`） | **D3D12/Metal 后端路径被卡住**：新后端不是"再加一个实现类"，而是先要还这笔债 |
| 2 | **Scene 组件层反向依赖 RHI**：`MeshComponent` 等直接持有 `unique_ptr<rhi::IRHIBuffer>`，`Scene/CMakeLists.txt` 链接 RHI | **ECS 无法脱离 GPU 复用**（服务器模拟、离线烘焙、headless 测试）；组件应只持 CPU 数据 + 句柄 |
| 3 | **序列化链路半残**：属性存盘已通（104 处 `HE_REGISTER_PROPERTY`），但反序列化仍靠两份硬编码 if-else，`ClassInfo::factory` 无消费方；类型分派表复制 3 份，已注册的 `u8` 属性静默不存 | **"新组件全链路"成本从 5 处涨到 8-12 处**；接线成本低（工厂已就绪），属高性价比项 |
| 4 | **性能目标未达标项**：Lumen L6（1080p/60fps）未达成并已暂停；计算 pass 的 CPU 录制耗时（16.4 ms）高于 GPU（3.4 ms） | 规划上需要**为"性能验收"设独立阶段**，否则特性会以"能跑但不够快"的状态累积 |
| 5 | **样板点未收敛**：`CollectLights` 序列化 switch 复制 3 份、三缓冲 SSBO 在 3 条管线各声明一份、渲染模式切换在 Sample 层复制（引擎无 PipelineRegistry） | 对应《技术全景》里"新增光源类型 8-12 处""新管线 5-6 处"的扩展成本；注：原计划中的**渲染类型注册表化 / CollectLights 抽取已由用户决定不做**（触发条件未到，编号 27/28 不复用） |
| 6 | **"已实现但不生效"的路径**：MSAA 的覆盖钩子零调用点（`DeferredPipeline.cpp:68-71` 只有一行日志 + 失效注释）；TAA 的 `GetJitterOffset()` 零调用点、`TAA_Resolve.frag.slang:30` 的 `jitterOffset` 只声明未用 ⇒ 抖动从未进入投影矩阵 | 比"功能缺失"更隐蔽：能力清单写着 ✅、面板能勾、代码齐全，但**实际不产生效果**；排查成本远高于编译错误。证据与修法见《HugEngine后处理与抗锯齿实现分析》§5.2/§6 |
| 7 | **Shader 依赖跟踪漏项**：`Shaders/Lumen/LumenFarField.slang` 与 `Shaders/Lumen/ScreenProbeSampling.slang` 被 4~5 个编译入口 include，却**不在** `SLANG_INCLUDES`（`Engine/Shader/CMakeLists.txt:264-281`），而每个 `.spv` 的 `DEPENDS` 只列该列表 ⇒ **改这两个文件不触发任何重编**、热重载也救不了（无 stage 后缀被静默 `continue`，`ShaderHotReload.cpp:177`） | **唯一一处"改了源码但构建产物与源码静默不一致"**：不报错、不告警、画面正常，只是行为与源码不符。修法便宜：补登记，或用 CMake 扫描 `#include "..."` 与 `SLANG_INCLUDES` 差集，让漏项在**配置阶段**失败 |
| 8 | **Shader 绑定号与共享结构无自动对账**：44 个 `kGPUBinding_*` ↔ 40+ 处手写 set 布局之间没有任何交叉断言，构建期不跑 `slangc -reflection-json`；`GBufferPushConstant`、`DeferredLightingPushConstant` 是唯二**没有 `sizeof` 断言**的共享结构；`Engine/RHI/RHI/Types.h:23-51` 是第二份手抄镜像且无对账 | 已有 **3 次翻车记录**（光照图键与 `kGPUBinding_ShadowMap0=4` 冲突触发 `VUID-...-binding-00279`、`kGPUBinding_Lumen=32` 需手工同步、`kStageMaskMesh` 与 `kStageMaskAmplification` 值互换致 mesh PSO 创建失败）。详见《HugEngine Shader系统实现分析》§5/§9 |
| 9 | **Shader 变体管理与热重载覆盖**：无 `-D`、无 specialization constant（160 个文件里唯一的功能性变体宏 `PTG_PERSISTENT` 恒为 0 ⇒ **持久化线程组路径不可达**）；热重载只认 `.vert/.frag/.comp`（`ShaderHotReload.cpp:67-86`），`.rgen/.rmiss/.rchit/.rahit/.rcall/.mesh/.task` 一律**静默跳过**、共享头改动也不级联 | 直接决定"能否在工程里安全改着色器"；另：146 个已编译 `.spv.h` 中有 **5 个无人使用**（`HybridRTPipeline` 时代遗留，可清理） |
| 10 | **物理查询未走 Jolt（两套世界语义并存）**：`CollisionSystem` 是纯 CPU 侧实现——不 include 任何 Jolt 头（`Engine/Scene/Scene/CollisionSystem.cpp:8-17`）、`Engine/Scene/CMakeLists.txt` 不链 Jolt、`Engine/Scene` 全目录无 Jolt 符号（已独立复核）；`Raycast` 对每个形状做 **O(N) 全量遍历**（`CollisionSystem.cpp:207-237`），Jolt 的 `NarrowPhaseQuery`/`CastRay` 完全未使用。叠加两处分歧：动态胶囊总高 $= \text{height}+2\,\text{radius}$ 而静态胶囊 $\approx \text{height}$（`Engine/Physics/PhysicsSystem.cpp:43` vs `:94`）；`TransformComponent.scale` 不参与 Jolt（`PhysicsSystem.cpp:61` 只读 pos/rot），但 CPU 侧半径乘最大缩放分量（`CollisionSystem.cpp:255-257`） | 查询开销随碰撞体数线性增长，且"物理世界"与"查询/调试世界"是两套形状语义；另：`RigidBodyComponent.enabled` 无消费者（`PhysicsSystem.cpp:143` 只剩注释）、`PhysicsWorld::Shutdown()` 全仓无调用者。详见《HugEngine Physics系统实现分析》§5–§8 |
| 11 | **资产管线两处口径断裂**：① **非索引静态图元被静默丢弃**——`LoadPrimitive` 只在 `prim.indices` 非空时填索引（`Engine/Asset/Asset/glTFLoader.cpp:349-357`）⇒ `MeshComponent` 索引数为 0 ⇒ 被合批与 GPUScene 直接跳过（`MeshBatcher.cpp:30-32`、`GPUScene.cpp:58`），资产"加载成功、组件也建了、什么都不显示、无告警"；**而同文件的蒙皮路径早已做兜底**（`:671-676`）⇒ 修法在库内已有先例。② **无引擎侧纹理资产层**：加载器只存 `image->uri`，解码与 bindless 登记全在 Sample 侧且 5 个示例各复制一份（`03.Sponza-Forward.cpp:341-412` 等），一律用 `RGBA8_UNORM`、**缺 sRGB 解码**（RHI 已有 `Format::RGBA8_SRGB`）；`emissive` 解析了却**没有 bindless 槽** | ①是"静默失败"，排查成本高、修法已有先例；②是"重复实现 + 颜色管线正确性"双重问题。详见《HugEngine 资产管线与glTF加载实现分析》§4/§5/§10 |
| 12 | **示例配置静默失效（实测缺陷，已独立复核）**：`Content/Config/` 下实有 `02_Cube.cfg`、`03_Sponza.cfg`、`04_Deferred.cfg`，但代码读取的是 `Config/03_Sponza-Forward.cfg`、`Config/04_Sponza-Deferred.cfg`（`03.Sponza-Forward.cpp:54`、`04.Sponza-Deferred.cpp:47`），05/06/07 读取的 `05_Sponza-PathTracing.cfg`/`06_GILab.cfg`/`07_Nanite.cfg` **根本不存在**（`05.Sponza-PathTracing.cpp:60`、`06.GILab.cpp:63`、`07.Nanite.cpp:66`）⇒ `hasConfig` 恒为 false，面板改动与窗口布局**不会持久化且无告警**。根因与示例重命名提交（`bf7114b` 03/04 改名）未同步 cfg 文件名一致 | 6 个示例中 4 个受影响；修法：统一 cfg 命名（或让代码读目录内唯一匹配项）并在缺失时打一条 WARN |
| 13 | **`.hescene` 存取往返实质不可用（两处独立缺陷，均已独立复核）**：① **Load 静默不建任何组件**——宏用 `#ClassName` 生成名字（`Engine/Reflect/Reflect/ReflectionMacros.h:46`），注册点写的是 `HE_BEGIN_REGISTER(he::TransformComponent)` 这类**带命名空间**的形式（`SceneReflect.cpp:44` 起），而 `Load` 拿 `cls->name` 去比**裸名**（`SceneSerializer.cpp:130-138` 的 `cn=="TransformComponent"` 等 8 个分支）⇒ 8 个分支**全部不可达**，走 `else { p+=ds; continue; }`；`LevelLoader::AddComponentByHash`（`LevelLoader.cpp:45-54`）同病（`WorldModel` 为此专门写了 `StripNamespace`，反证了这层缺失）。② **Save 静默不写绝大部分属性**——注册点的类型名是 `float`（`SceneReflect.cpp:79` 等，共 52 条），而存盘分派表只认 `"f32"`（`SceneSerializer.cpp:32`）⇒ **52 条 `float` 属性一个字节都不写**（`int`/`u8`/`float2` 同族也不认） | 这是**当前最严重的功能性缺口**：编辑器保存的场景无法正确还原（实体在、组件空、数值丢）。根因是"同一份类型名/类名在两处以不同写法被手工对齐"——与 §16-8（绑定号无自动对账）同源。详见《HugEngine 反射与序列化实现分析》§2.4/§6.3/§7.3 与《HugEngine 编辑器实现分析》§6 |
| 14 | **编辑器视口绕过完整管线**：编辑器只调 `NextFrame`/`BeginFrame`/`RenderScene`（`EditorApp.cpp:378-379`、`ViewportPanel.cpp:52,68`），**从不调用 `ForwardPipeline::Render`**（已独立复核：全目录无管线 `Render()` 调用点，同名命中全是 ImGui 面板的 `Render()`）⇒ 视口**无阴影、无天空盒、无 ToneMap、无 GI、无 GPU 剔除**，`render.aa_mode` 的 AA 对象虽被创建替换但该路径不渲染。同批编辑器侧问题：Undo 只覆盖 **21 处 `PropertyChangeCommand`（全在 DetailsPanel）+ 1 处 `GenerateSceneCommand`**，增删实体/`Add Component`/Gizmo 拖拽/导入与场景加载均不可撤销，且撤销栈 lambda 捕获组件裸指针而删实体/载场景都不清历史；10 个编辑器 CVar 中 **4 个是死开关**；`LevelLoader` 从 UI 不可达（`levelPath` 无赋值点） | "编辑器看到的东西 ≠ 运行时的东西"，最容易造成"改了没效果"的误判；详见《HugEngine 编辑器实现分析》§3/§7/§10 |
| 15 | **验证入口不是一等公民，且脚手架无法从克隆重现**：① 全仓**无 `add_test`**（`ctest` 恒 0 用例）、`CMakePresets.json` 无 `buildPresets`/`testPresets`、测试默认 **OFF** ⇒ 想跑一次单测必须先重新 configure；**无 CI**。② `README.md:21` 的 `cmake --build Build` 在多配置生成器下默认构建 **Debug**，而所有验证脚本默认找 **Release**（`Tools/gi/dump_gi.ps1:35,40`），且现有 `Build/` 是含改名前的旧目标的过期树。③ `Tools/gi` 有 **16 个脚本**把 `Content/Config/06_GILab.cfg` 当必需基线并 `throw`（`dump_gi.ps1:42-46`），而该文件由示例**退出时**写出、`Content/Config/` 被 gitignore；④ **Nanite 的整套验收脚本（7 个）与 `HE_SMOKE_EXTRA` 既不在版本控制、又含硬编码绝对路径**，`Tools/` 下没有任何引用 `07.Nanite.exe` 的脚本 ⇒ 其判据事实上无法从克隆重跑。另：`CMakeLists.txt:123` 的示例分组**漏了 `07.Nanite`**（同文件 `:104-105` 注释要求列全） | 直接决定"新人能否复现一次验证"；建议：补 `add_test`/`testPresets`、README 统一 `--config Release`、把验证脚手架移入受跟踪目录并改为从仓库根推导路径。详见《HugEngine 开发与验证手册》§2/§5/§8 |
| 16 | **依赖治理没有单一事实来源（两套引入方式混杂）**：`Engine/External/` 下 11 个库中只有 **7 个是 git 子模块**（`.gitmodules:1-21`），另外 4 个（doctest / imgui / nlohmann / VMA）是**直接入库的副本**且**都没有 LICENSE 文件**（已独立复核：7 个子模块库均带 LICENSE，这 4 个均无）；`Engine/External/CMakeLists.txt:1-4` 却声称"git submodules（无网络依赖）"，而 configure 期还会 `git clone` Sponza（`cmake/DownloadAssets.cmake:12-27`，失败仅 WARNING）。**无任何依赖清单/lockfile**：`.gitmodules` 只记 URL 不 pin commit，Vulkan SDK 取"本机最新"，`GLM_TEST_ENABLE`（`External:16-18`）是**无效选项**（真实名 `GLM_BUILD_TESTS`）。**版本双写**：Jolt `Core.h:8-10` 写 5.6.1 而 `Build/CMakeLists.txt:3` 写 5.6.0，运行期日志取前者。另：VMA/cgltf/doctest **没有 CMake 目标**（仅 include 路径）、taskflow/meshoptimizer **无条件**注册 install/export（taskflow 还会 `export(PACKAGE)` 写用户级包注册表）、`External/CMakeLists.txt:68` 的注释指向不存在的路径 | 升级/回退依赖时没有权威版本来源；第三方许可证缺失对**分发**是硬风险。建议：新增依赖清单文件（库 \| 版本 \| pin commit \| 来源 \| 许可证 \| 更新方式）+ 一条校验脚本断言"vendored 目录必有 LICENSE、版本头与清单一致、每个 target 都在根分组清单里"。详见《HugEngine 构建与依赖管理》§4/§5/§8 |

---

## 第六部分 · AI 与规划方法论

### 17. AI 规划的配套缺口

`HugEngine AI架构设计/` 系列设计超前（AI 与 RHI 平级、反射即世界模型、动作走 Command 可撤销），但配套缺失：

| # | 缺口 | 说明 |
|---|------|------|
| 1 | **安全边界** | LLM 输出 JSON 有类型校验，但无 **prompt injection 防御、资源爆炸防护**（LLM 生成 1 万实体 / 超大纹理怎么办）、执行限额 |
| 2 | **AI 回归测试** | ⚠️ *部分落地*：`Tests/` 已有结构性断言单测（`TestSceneBuilder` / `TestPromptToScene` / `TestWorldModel` / `TestAgentAction` / `TestAgentBrain` / `TestAIPipeline` / `TestAIGCProvider` / `TestDeepSeekProtocol`）；**仍缺**：生成结果的渲染级/金图回归与"生成不破坏引擎"的集成测试 |
| 3 | **模型与成本管理** | ⚠️ *部分落地*：`IAIDevice` 已按模型格式分派 GPU / CPU / Remote 后端，`AIPipeline` 有失败重试与同 prompt 去重合并，`InferenceScheduler` 有优先级车道；**仍缺**：每帧 token / 显存预算上限与成本计量；`6.LLM与引擎通信协议` 未覆盖 |

### 18. 规划方法论问题

| # | 问题 | 说明 |
|---|------|------|
| 1 | **无验收标准** | 350+ 项特性几乎没有可验证指标（"Nanite 支持 1B 三角 @ 60fps？""VSM 页表命中率目标？"）；原《架构设计与任务划分》（2026-09 已删除）有金图测试，但验收标准没有落到每个特性 |
| 2 | **优先级失衡** | 按《技术全景》正文逐条清点：🔴 核心 93 项、🟡 重要 172 项、🟢 进阶 86 项（共 351 条）——三档分布与 8 个 Phase 的排期并不对应；建议区分**研究型（证明概念）**与**产品型（可交付）** |
| 3 | **无对标基线更新机制** | 文档写"对标 UE5.5+"，但无持续追踪 UE6 / Godot 4.x / Unity 6 新特性的流程，规划会逐年过期 |
| 4 | **规划碎片化** | 技术全景 / 《架构UML与可扩展性分析》 / HugEngine AI架构设计 三套体系仍无单一"路线图"文档做索引（如 `7.LLM创建场景实现计划` 引用了不在主清单中的 AI 模块）。文档集已于 2026-09 做了一次收敛：`docs/HugEngine开发进度.md`（状态断言型，多项"未完成"此后已落地）与 `docs/HugEngine架构设计与任务划分.md`（145 模块 WBS / 依赖拓扑 / 测试策略 / 风险 / 人力配置）**均已删除**；《架构UML文档》与《架构可扩展性分析》**已合并**为《HugEngine架构UML与可扩展性分析》，承担"架构总览 + 可扩展性评估"的索引角色——但《技术全景》仍是唯一特性清单，WBS 与测试策略缺承接文档 |
| 5 | **同一实现在多份文档里给出不同数字** | 本次新写的《硬件光追与路径追踪实现分析》逐项对照发现：`PathPayload` 在《全路径追踪管线规划》中 **48B/96B/112B 三档并存**，而代码是 **112B**（`PathPayload.h:37`）；PTPass 的 UAV 数文档写 4 / 代码 5；场景材质纹理行数文档 4/7/8/11、代码 11；轮盘赌起点文档 3 / 代码 2；STBN 格式文档 `R8G8B8`+`SampleLevel` / 代码 `RGBA8_UNORM`+`Load`；ReSTIR 候选数 M 文档 8 / 代码 16。**建议**：对易漂移的量建立"单一事实来源 + 校验脚本"——仓库已有先例（`Tools/check_path_payload.py`、各文档的 `check_tables` 复核），把它扩成"发布前跑一次全库数字对账"即可覆盖 |

---

## 第七部分 · 行动清单与附录

### 19. 补入优先级（P0 / P1 / P2 / P3）

> **本表由两套优先级合并而成**：原 RHI 文档 §9.1（P0–P3，17 行）与原规划文档 §9（P0 / P1 / P2 行动清单，28 项）合成一张，按 P0→P3 排列，同一缺口只出现一次；合并行在「来源」列标注。原规划文档 §9 的勾选状态（`[x]` / `[ ]`）逐项保留在「状态」列；RHI 侧行在原文档中没有勾选状态，记 `—`。
> **优先级口径**：**P0** — 进 Phase 1-2，否则影响其他架构决策；**P1** — Phase 3-4；**P2** — 后续阶段（原规划文档）；**P3** — 原 RHI 文档 §9.1 的末档（长期项）。

| 优先级 | 缺口 / 功能 | 理由 / 说明 | 来源 | 状态 |
|--------|-------------|-------------|------|------|
| **P0** | 动态渲染 (`VK_KHR_dynamic_rendering`) | 消除 VkRenderPass 的复杂性，简化 RenderGraph 实现 | RHI §9.1 | — |
| **P0** | 资源状态追踪 + 自动 Barrier | 手动 Barrier 易出错，是正确性和性能的基础；**基础版已实现**（`RenderGraph::DeriveBarriers()` 自动推导 + 状态去重），RHI 层仍是手动接口 | RHI §9.1 | — |
| **P0** | D3D12 后端 + 后端抽象纪律（合并行） | 覆盖绝大多数 Windows 用户的 GPU (NVIDIA/AMD/Intel)；前置动作（原规划 P0）：清掉 `GI_RSM` 的 Vulkan 死依赖与 `ImGuiIntegration` 的 `VulkanDeviceAccess`，把 RHI 的 `void*` 逃逸口逐个换成强类型抽象——否则 D3D12/Metal 无从落地（证据见《架构UML与可扩展性分析》§17 P0-1） | RHI §9.1 + 规划 §9 | — |
| **P0** | PSO 缓存 (磁盘持久化) | 减少启动停顿，用户直接体感；**基础版已实现**（VkPipelineCache 持久化 + GPL 四段库 + 创建限流），剩余的是 base/derived pipeline 派生 | RHI §9.1 | — |
| **P0** | 物理抽象层 + 选型（**已选 JoltPhysics 并落地**，见 `Engine/Physics`），物理 tick 与渲染 tick 耦合设计（`PhysicsSystem::Update` 固定步长 1/120s + Transform 回写） | — | 规划 §9 | [x] |
| **P0** | 时间系统（固定步长 / 时间膨胀 / 回放时间线） | — | 规划 §9 | [ ] |
| **P0** | DebugDraw 调试渲染原语（**部分落地**：`CollisionDebugSystem` 碰撞体线框 + `TextRenderSystem` 世界空间文字；仍缺通用 `DrawDebugLine/Box/Sphere` 即时原语） | — | 规划 §9 | [x] |
| **P0** | 输入抽象层（Input Action / 键位绑定） | — | 规划 §9 | [ ] |
| **P0** | 运行时 AssetManager（异步加载 / 依赖图 / 引用计数） | — | 规划 §9 | [ ] |
| **P0** | GPU 内存预算管理器设计文档（VSM/VT/Nanite/3DGS/RT 共存） | — | 规划 §9 | [ ] |
| **P0** | Scene 组件与 GPU 资源解耦：组件只持 CPU 数据 + 句柄，GPU 缓冲上移到 Render/Asset —— 顺带收敛"逐类型 ForEach"的根因（§17 P0-2） | — | 规划 §9 | [ ] |
| **P0** | 序列化工厂接线：`SceneSerializer::Load` 改用 `ClassInfo::factory`，3 份类型分派表收敛到一处并补齐 `u8` 等已注册类型 —— 成本最低、见效最快（§17 P0-3） | — | 规划 §9 | [ ] |
| **P1** | RenderGraph 完善（图可视化/Pass合并优化/显式外部依赖） | 已有基础 RG，需继续完善 | RHI §9.1 | — |
| **P1** | 扩展动态状态 | 大量减少 PSO 变体数量，降低编译开销 | RHI §9.1 | — |
| **P1** | Profiler / Debug Markers（GPU + CPU，含 Tracy 集成）（合并行） | RHI 侧：**基础版已实现**（`ProfilerManager` 逐 Pass 时间戳 + `VK_EXT_debug_utils` 标签），缺 Tracy/RenderDoc 集成与 GPU 崩溃定位；规划侧：Tracy CPU Profiler 集成——现状"只有 GPU 时间戳 + ImGui 面板" → 直接规划 **Tracy** 集成（CPU 采样 + 内存追踪），现代引擎标配；另补内存碎片分析、帧分析器 | RHI §9.1 + 规划 §9 | [ ] |
| **P1** | Upload Heap (环形缓冲) | 消除每帧创建/销毁 staging buffer 的开销 | RHI §9.1 | — |
| **P1** | 脚本运行时决策（Lua / C# / 自研 VM 三选一）+ 游戏框架（GameMode/PlayerController/Pawn） | — | 规划 §9 | [ ] |
| **P1** | 运行时 UI 系统（Canvas / 布局 / 事件 / 数据绑定） | — | 规划 §9 | [ ] |
| **P1** | 游戏 AI：**寻路已落地**（自研网格 8 向 A*：`NavMeshSystem` + `NavAgentSystem`，替代原 Recast/Detour 建议）；**仍缺**行为树 / 状态机决策与感知系统 | — | 规划 §9 | [x] |
| **P1** | 动画状态机（AnimGraph）/ BlendSpace / IK / 物理动画（**骨骼网格播放 / 剪辑混合 / 动画重定向已落地**，见 §13 第 6 项） | — | 规划 §9 | [ ] |
| **P1** | Sequencer 过场系统 | — | 规划 §9 | [ ] |
| **P1** | CI / 金图测试落地（GitHub Actions + 截图对比 + 夜间回归） | — | 规划 §9 | [ ] |
| **P1** | 色彩管理 / OCIO 全链路设计 | — | 规划 §9 | [ ] |
| **P1** | Cook / 打包 / 热更管线 | — | 规划 §9 | [ ] |
| **P2** | MSAA（**已有实现，属"修通"而非"从零做"**） | `AA_MSAA` 的上层描述符覆盖已接线（`DeferredPipeline.cpp:68`）；需修的是 RHI 默认 PSO 路径与预热路径的采样数不一致（见 §4.2 注） | RHI §9.1 | — |
| **P2** | Subpass Input Attachment | 移动端性能优化关键 | RHI §9.1 | — |
| **P2** | Geometry/Tessellation Shader | 兼容旧内容，特定效果仍有需要 | RHI §9.1 | — |
| **P2** | VRS / Conservative Rasterization | 性能优化手段 | RHI §9.1 | — |
| **P2** | 水系统、VHM 地形（Nanite 地形合一） | — | 规划 §9 | [ ] |
| **P2** | USD / MaterialX / FBX 支持 | — | 规划 §9 | [ ] |
| **P2** | DirectStorage / GPU 解压（与 SVT/VSM 的 IO 层设计配套） | — | 规划 §9 | [ ] |
| **P2** | 插件 / 模块系统、Live Coding | — | 规划 §9 | [ ] |
| **P2** | 移动端（ASTC/TBDR/功耗）与主机平台抽象（合并行） | 规划侧：只提了 PSSR；PS5/Xbox 的内存对齐、SDK 抽象零规划。RHI 侧：Mobile Vulkan（移动平台扩展）现状为「仅 Win32，使用 Vulkan 1.3+ 特性」（详见 §11）——原 RHI 列为 P3，此处按合并后条目的整体范围归入 P2 | RHI §9.1 + 规划 §9 | — |
| **P2** | 像素流送 / 云渲染 | — | 规划 §9 | [ ] |
| **P2** | NVIDIA 专属特性降级路径（RTXNS/NRC/RTXTS 的 CPU/算法回退） | — | 规划 §9 | [ ] |
| **P2** | 崩溃处理 / 遥测（合并行） | 崩溃处理（`Engine/Core/CrashHandler`：DbgHelp 符号化 + minidump）**已落地**；遥测采集 / GPU fault 与设备丢失诊断仍缺。RHI 侧：GPU 崩溃定位对标 UE5 `BreadcrumbContext` 记录 GPU 进度、崩溃时定位问题命令 → HugEngine 无（该事实原列于 P1「GPU Profiler / Debug Markers」行） | RHI §9.1 + 规划 §9 | [x] + [ ] |
| **P2** | AI 配套：安全边界（prompt injection / 资源爆炸防护）、AI 渲染级回归、模型与成本管理 | 明细见 §17 | 规划 §9 | [ ] |
| **P2** | **性能验收阶段化**：为"已落地但未达标"的特性（如 Lumen L6 1080p/60fps 暂停项）设独立阶段与判据，避免以"能跑但不够快"累积 | 明细见 §16 第 4 行 | 规划 §9 | [ ] |
| **P3** | Inline Ray Tracing | 提升 RT 灵活性 | RHI §9.1 | — |
| **P3** | SER / OMM | 硬件 RT 加速 | RHI §9.1 | — |
| **P3** | Cooperative Vectors / Linear Algebra | ML 推理集成 | RHI §9.1 | — |
| **P3** | Multi-GPU / VR | 特殊场景需求 | RHI §9.1 | — |

> 注：P1「Profiler / Debug Markers」行内的"GPU 崩溃定位"为原 RHI §9.1 原文；其条目化处理在 P2「崩溃处理 / 遥测」行与 §14.2 第 6 行，此处不再单列一行。RHI 侧原 P3 的「Mobile Vulkan」已并入 P2「移动端（ASTC/TBDR/功耗）与主机平台抽象」合并行。

### 20. 明确不需要的（UE5 RHI 中）

- **Transform Feedback (Stream Output)** — Vulkan 已不推荐，Mesh Shader 替代
- **Immediate Mode RHI** — UE5 的即刻提交模式，HugEngine 统一使用 CommandList 模式更简洁
- **OpenGL 后端** — 已被业界淘汰
- **D3D11 后端** — Vulkan 1.3+ 和 D3D12 已覆盖所有目标平台

### 21. 对照检查表：缺口 → 应更新的规划文档

| 缺口 | 应更新到 | 建议动作 |
|------|----------|----------|
| 物理 / 游戏框架 / 导航寻路 / DebugDraw（**已落地但未回写特性表**）+ 音频 / 运行时 UI / NPC 决策与感知 / 输入 / 通用调试原语（仍缺） | `技术全景` 新增特性族（如 Section 26 游戏层）+ 架构规划文档新增模块（M146+） | 先把已落地项补进特性表，再补缺失项 + 依赖 + Phase |
| 时间系统 / AssetManager / 内存预算 / 确定性 | 架构规划文档新增横切设计章节（Section 9） | 各写一份设计文档 |
| 动画状态机 / IK / 水系统 / VHM / DirectStorage | `技术全景` 对应 Section 18/12/3 增补条目 | 补特性表 |
| USD / MaterialX / Cook / 色彩管理 | `技术全景` Section 20/23 增补 | 补特性表 |
| Tracy / CI / Live Coding / 插件 / Sequencer / 崩溃遥测 | `技术全景` Section 23/24 + 架构规划文档测试策略 | 补特性表 + WBS |
| AI 安全 / 测试 / 成本 | `HugEngine AI架构设计/3.AI统一基座设计规格`、`6.LLM与引擎通信协议` | 增补章节 |
| 验收标准 / 优先级再平衡 / 对标更新机制 / 单一路线图 | `技术全景` 与架构规划文档总览部分 | 建立每特性验收字段 + 季度对标刷新 |

> 注：表中"架构规划文档"指承担原《HugEngine架构设计与任务划分》职责的文档——该文档（145 模块 WBS / 依赖拓扑 / 测试策略 / 风险 / MVP 裁剪 / 人力配置）已于 2026-09 删除。**现状**：《HugEngine架构UML文档》与《HugEngine架构可扩展性分析》已合并为 `docs/HugEngine引擎介绍/02.架构UML与可扩展性分析.md`（架构总览 + 分层类图 + 关键时序 + 可扩展性评估/代码级债务），可承接"架构"一侧的引用（§16 即引用其 §17）；**WBS / 测试策略 / 风险登记 / 人力配置仍无承接文档**。

### 22. 参考来源与文档版本

参考来源文档：

- `docs/HugEngine引擎介绍/01.技术全景与实施计划.md`（350+ 项特性清单，v3.1）
- `docs/HugEngine引擎介绍/23.AI架构设计/1.HugEngine AI一等公民架构设计.md`
- `docs/HugEngine引擎介绍/23.AI架构设计/2.HugEngine AI统一基座设计.md`、`3.AI统一基座设计规格.md`
- `docs/HugEngine引擎介绍/23.AI架构设计/4.HugEngine AIGC创作平台设计.md`、`6.LLM与引擎通信协议.md`

- `docs/HugEngine引擎介绍/02.架构UML与可扩展性分析.md`（架构总览 / 分层类图与关键时序 / 可扩展性评估与代码级债务清单 —— §16 与 P0 债务项的证据来源）
- `docs/已实现功能/`（20 份落地设计与判据：Nanite / Lumen / GI 架构 / Jolt 物理 / Entity Component / 编辑器 Phase3 / 全路径追踪 / 基于物理的渲染管线 / TAA / Bindless / 瞬态分配器 / VMA / Shader 热重载 等）
- `Tests/`（46 个测试文件 / 328 个 TEST_CASE，覆盖物理、AI、Lumen、Nanite、导航、反射与组件）

源文档信息（合并前）：

> 注：两份源文档（`docs/HugEngine引擎介绍/HugEngine RHI功能缺口分析-与UE5对比.md`、`docs/HugEngine引擎介绍/HugEngine规划缺口分析-对标2026游戏引擎.md`）已于 2026-09 合并进本文件并从仓库删除；正文中的"原 RHI 文档 §x"与"原规划文档 §x"即指这两份，用于追溯每条结论的来源。需要旧文全文时用 git 历史取回。

**1) RHI 文档：《HugEngine RHI 相对于 UE5 的功能缺口分析》**

- 分析日期：2026-09-21；复核更新 2026-09-22（① 修正 MSAA 判断——**核实为"代码齐全但从未生效"**：覆盖钩子零调用点、RHI 运行时 PSO 硬编码采样数 1、预热路径读该字段且为裸 `static_cast`；② 把 RenderGraph 更正并入正文，§十 只留实现位置与差距清单；③ 补记实测证据；④ 依《后处理与抗锯齿》《Shader 系统》两篇新文档补入 §16 的 4 条代码级债务）
- 姊妹文档（RHI 架构与 Vulkan 实现细节）：[14.RHI架构与Vulkan实现分析.md](14.RHI架构与Vulkan实现分析.md)
- 判定口径说明：已逐字保留于本文件文首（原 RHI 文档头部）

**2) 规划文档：《HugEngine 规划缺口分析 — 对标 2025-2026 游戏引擎架构》**

- 最后更新：2026-09-22（依据 2026-09 源码与归档文档复核：① 组件与玩法族任务 1~26 全部收口并归档；② 补入 Nanite / Lumen / GI 分层合成 / 骨骼与重定向的落地状态；③ 新增"架构债务"小节，指向《架构UML与可扩展性分析》§17；④ 参考来源与文档集变动同步：原《HugEngine开发进度》《HugEngine架构设计与任务划分》已删除，新增《HugEngine架构UML与可扩展性分析》）
- 文档版本：v1.1（2026-09-22）
- 性质：规划咨询产出，供补充《技术全景》与后续架构规划文档时引用；不替代上述文档。
