# 最近 17 个提交代码审查报告

> 审查范围：`HEAD~17..HEAD`（`0c75eb5` → `1124f48`）
> 审查方式：/code-review high + 逐行人工核对
> 结论：问题高度集中在 `9b6e612 M4.1 halfRes` 提交，半分辨率路径存在 3 个已确认 Bug，另有 2 个一致性风险。

---

## 提交概览

| 类别 | 说明 |
|------|------|
| 纯格式化 | `c536951`（每语句一行，约 80 文件 558 行）——占改动绝大多数，未见功能问题 |
| 功能改动 | 集中在 GI（SSGI/SSR/DDGI、GIConfig、GIRegistry）、LightingPass 重构（33 参数 → 结构体）、帧图编排、阴影修复 |

改动统计：102 文件，+3299 / -1007。

---

## 🔴 严重（halfRes 路径直接损坏）

### 1. Lighting 绑定的纹理和帧图注册的纹理不一致

**位置**：`Engine/Render/Pipeline/DeferredPipeline_FrameGraph.cpp:447 / 449`

halfRes 时跳过 Denoise，帧图里把 `ssgiDenoised = ssgiOut`（原始 SSGI 半分辨率纹理）注册为 Lighting 的 Read 依赖并加屏障。但 Lighting lambda 里是**无条件**绑定 Denoise 输出：

```cpp
in.ssgiTex = m_DenoiseSSGI.GetOutput();   // 447：halfRes 下这张本帧从没被写过
in.ssrTex  = m_DenoiseSSR.GetOutput();    // 449：同上
```

**后果**：选 Low 档（halfRes=true）时，屏障加在 `ssgiOut` 上，Lighting 实际采样的却是 `m_DenoiseSSGI.GetOutput()`——一张本帧无生产者、无 shader-read 屏障的陈旧/未初始化纹理。整个 Low 档的间接漫反射 + 反射变成冻结/垃圾画面。

**修法**：halfRes 时 `in.ssgiTex/in.ssrTex` 应指向 `m_SSGI/m_SSR.GetIndirectDiffuse/SpecularTexture()`，与 `ssgiDenoised/ssrDenoised` 保持同一张。

---

### 2. GI_SSGI 视口用全分辨率，但渲染目标是半分辨率（SSR 修了，SSGI 漏了）

**位置**：`Engine/Render/GI/GI_SSGI.cpp:158`

```cpp
// GI_SSGI::Render —— 用 m_Width/m_Height（全分辨率）
cmd->SetViewport({0, (float)m_Height, (float)m_Width, -(float)m_Height, 0, 1});
cmd->SetScissor({0, 0, m_Width, m_Height});
```

而 `CreateOutputTex(halfResW(width), …)` 输出是半分辨率，帧图也用 `ssw/ssh`（半）调 `BeginOffscreenPass`。对比 `Engine/Render/GI/GI_SSR.cpp:106-108` 已修成用输出纹理实际尺寸：

```cpp
u32 ow = m_Output->GetWidth();  // SSR 正确做法
u32 oh = m_Output->GetHeight();
cmd->SetViewport({0, (float)oh, (float)ow, -(float)oh, 0, 1});
```

**后果**：视口是渲染区的 2 倍，全屏三角把 NDC 映射到 2×2 区域，只有左上 1/4 落进渲染区——半分辨率 SSGI 被渲染成 2× 放大的裁剪画面。

**修法**：SSGI 也改用 `m_Output->GetWidth()/GetHeight()`。

---

### 3. 未初始化的 ResourceHandle 被当 Read 依赖传入

**位置**：`Engine/Render/Pipeline/DeferredPipeline_FrameGraph.cpp:338 / 300`

```cpp
render::ResourceHandle ssgiDenoised;   // 338：无初始化
render::ResourceHandle ssrDenoised;    // 300：无初始化
```

当 `ShouldRunSSGI()/ShouldRunSpecular()` 为 false（如 RT 硬件的 Ultra 档用 RTGI），这俩从不赋值，但 399-400 行**无条件**注册 `{ssgiDenoised, Read}/{ssrDenoised, Read}`，用了个不确定的 handle 值，导致帧图索引任意/越界资源——错误屏障或崩溃。

**佐证**：同文件 170/174 行处理 `csmMaps`、`spotShadowHandle` 时都显式用了 `kInvalidHandle`——唯独这两个漏了。

**修法**：初始化为 `kInvalidHandle`，并在通道禁用时不注册对应 Read 项。

---

## 🟡 中（潜在，特定配置触发）

### 4. RSM 守卫条件改弱，可能在无方向阴影时误触发

**位置**：`Engine/Shader/Shaders/DeferredLighting.frag.slang:257`

守卫从 `shadowParams.z > 0`（阴影强度）改成 `lightCount > 0u && shadowParams.w < 0.5f`。当场景有光但没有方向阴影投射者时，`u_ShadowData[0]` 全零 → `shadowParams.w==0`（< 0.5，恰好匹配方向光编码 0）→ RSM 用零矩阵跑，`lsp.w==0` 导致 `rsmUV` 走 0/0，给间接光注入 NaN/杂色。

> 注：此项在 shader 内，逻辑成立但未逐指令验证矩阵零值路径，建议确认。

**修法**：恢复对阴影强度字段的守卫，或改为对首个方向阴影条目是否真实填充的判断。

---

### 5. DDGI 有两个真值来源，可能不同步

**位置**：`Engine/Render/Pipeline/DeferredPipeline_FrameGraph.cpp:453`

Pass 注册用 `m_GIConfig.ShouldRunDDGI()`（262 行），overlay 标志却用 `m_DDGI.IsEnabled()`（453 行）。两者目前靠 06.GILab 手工保持一致；一旦某个 preset 让 overlay=false 但 `IsEnabled()` 仍 true，`DDGI_Update` 被跳过而 shader 仍采样从未更新的探针 → 叠加陈旧间接光。

**修法**：`in.ddgiOverlay` 从与 Pass 门控相同的 GIConfig 源派生，而非单独维护 `m_DDGI.IsEnabled()`。

---

## 小结

问题**高度集中在 halfRes 特性**（前 3 个都是它）：新增了半分辨率纹理，但视口、纹理绑定、帧图句柄三处只改了 SSR、漏了 SSGI，且 Lighting 绑定端完全没跟上 halfRes 分支。剩下两个是 GI 状态"双真值源"引入的一致性风险。

其余约 80 个文件的改动几乎都是 `c536951` 的纯格式化（每语句一行），未见功能性问题。

**建议优先级**：#1、#2、#3 三个确定 Bug 应尽快修复，改动均很局部。
