// ============================================================
// Shadow.frag.slang — 阴影贴图片元着色器
//
// 空着色器：仅写入深度，不需要颜色输出。
// ============================================================

struct FSInput {
};

// 注：阴影通道无颜色附件，片元着色器不输出任何颜色
// 深度由固定管线写入（pipeline depthWrite = VK_TRUE）
void fragmentMain(FSInput input) {
}
