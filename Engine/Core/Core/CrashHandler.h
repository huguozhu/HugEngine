#pragma once

// ============================================================
// Core/CrashHandler.h — 崩溃处理器（诊断基建，对应计划 Wave 0.8 / D1）
//
// 目的：把"偶发崩溃只能靠 WER 的错误偏移反查"变成"崩溃时直接打出完整调用栈"。
//
// 背景：06.GILab 曾出现约 1/55 的偶发访问违例（0xC0000005），WER 只给出
// 模块内偏移（0x38216b），需要手工用 /MAP 反查才定位到
// VulkanTexture::GetImageView()。有了本处理器，下次崩溃可直接读到
// "函数名 + 源文件:行号" 的调用栈，定位成本从"猜"变成"读"。
//
// 同时会写出 minidump（.dmp），可用 Visual Studio 直接打开查看崩溃现场。
//
// 【为什么放在引擎里而不是示例里】（D1 / 任务 2）单元测试可执行文件同样需要一个
// 可信的崩溃出口 —— 实测它在"只跑部分用例"时**每次都在退出阶段崩溃**（0xC0000005、
// 出错模块 unknown、错误偏移 0，即跳到了地址 0）。把它下沉到 Core 后示例与测试共用
// 同一份实现，崩溃报告不再各自为政。
//
// 注意：处理器在记录完成后默认返回 EXCEPTION_CONTINUE_SEARCH，**故意让系统默认
// 处理（WER）继续执行**——这样既拿到我们自己的调用栈，也保留 WER 的 APPCRASH
// 事件记录，两种证据都不丢。自动化场景（soak / 自检）可设 `HE_CRASH_NO_WER=1`
// 让进程立即以退出码 2 结束：WER 收集转储很慢（实测默认路径要 62 秒才退出）。
// ============================================================

namespace he {

/// 安装崩溃处理器
/// logPath ：崩溃日志路径（追加写入）；传 nullptr 表示只输出到 stdout
/// dumpPath：minidump 输出路径；传 nullptr 表示放在可执行文件同目录
void InstallCrashHandler(const char* logPath, const char* dumpPath);

} // namespace he
