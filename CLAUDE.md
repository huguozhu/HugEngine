# HugEngine 项目规则

## Git 规则
- `git commit` 默认直接执行，不再询问用户确认。达到可提交状态（构建通过、相关验证完成）
  即可提交；不要提交未验证或半成品的工作。
- 默认**按逻辑分开提交**，不要合并成一条。每条提交只做一件事，便于单独回退。
  提交信息与分组可直接给出，无需先征求确认。
- `git push` 仍需用户明确要求，不自动执行。
- Commit log 使用中文。
- Commit log 不得包含 AI 相关信息（如 Co-Authored-By、Generated with Claude Code 等）。
- Commit log 不得包含无效信息（如 `* @` 等格式字符）。

## 代码规范
- 添加代码时必须附带对应的中文注释。
